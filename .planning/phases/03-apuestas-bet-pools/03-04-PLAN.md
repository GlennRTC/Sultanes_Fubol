---
phase: 03-apuestas-bet-pools
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/App.tsx
  - src/components/Navbar.tsx
autonomous: true
requirements: [APU-02, APU-07]

must_haves:
  truths:
    - "Navigating to /apuestas shows the ApuestasPage for authenticated users"
    - "Unauthenticated users navigating to /apuestas are redirected to /login"
    - "Navbar shows Calendario | Apuestas | Tabla in that order"
    - "Apuestas NavLink is active (text-green-400) when on /apuestas"
    - "Apuestas NavLink is inactive (text-slate-300) when on other routes"
  artifacts:
    - path: "src/App.tsx"
      provides: "/apuestas route inside ProtectedRoute layout"
      contains: "ApuestasPage"
    - path: "src/components/Navbar.tsx"
      provides: "Apuestas NavLink between Calendario and Tabla"
      contains: "Apuestas"
  key_links:
    - from: "src/App.tsx"
      to: "src/pages/ApuestasPage.tsx"
      via: "React Router Route element"
      pattern: "path=\"/apuestas\""
    - from: "src/components/Navbar.tsx"
      to: "/apuestas route"
      via: "NavLink to=/apuestas"
      pattern: "to=\"/apuestas\""
---

<objective>
Wire ApuestasPage into the app: add the /apuestas protected route to App.tsx and add the "Apuestas" NavLink to Navbar.tsx between "Calendario" and "Tabla".

Purpose: The final wave that makes the complete /apuestas feature accessible. Both changes are small surgical insertions — no structural changes to existing routing or navigation logic.
Output: Updated src/App.tsx (one new Route), updated src/components/Navbar.tsx (one new NavLink).
</objective>

<execution_context>
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-apuestas-bet-pools/03-CONTEXT.md
@.planning/phases/03-apuestas-bet-pools/03-UI-SPEC.md
@src/App.tsx
@src/components/Navbar.tsx
@.planning/phases/03-apuestas-bet-pools/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /apuestas route to App.tsx and Apuestas NavLink to Navbar.tsx</name>
  <files>src/App.tsx, src/components/Navbar.tsx</files>
  <action>
Two surgical changes only. Do not alter any existing import, route, or JSX structure beyond what is described here.

**src/App.tsx changes:**

1. Add import at the top with other page imports:
   import { ApuestasPage } from './pages/ApuestasPage';

2. Inside the protected route layout block (the Route element that renders Navbar + main), add the /apuestas route directly after the /tabla route and before the / redirect:
   Current order: /calendario, /tabla, /, /bienvenido
   New order: /calendario, /tabla, /apuestas, /, /bienvenido

   Insert:
     <Route path="/apuestas" element={<ApuestasPage />} />

   This route is inside the ProtectedRoute element — unauthenticated users are redirected to /login by the existing ProtectedRoute guard (no changes needed to ProtectedRoute).

**src/components/Navbar.tsx changes:**

Insert one NavLink between the existing "Calendario" NavLink and the existing "Tabla" NavLink. The new NavLink follows the exact same className pattern as both existing NavLinks:

    <NavLink
      to="/apuestas"
      className={({ isActive }) =>
        isActive
          ? 'text-sm text-green-400 px-3 py-1 rounded-lg min-h-[44px] flex items-center'
          : 'text-sm text-slate-300 hover:text-white px-3 py-1 rounded-lg min-h-[44px] flex items-center'
      }
    >
      Apuestas
    </NavLink>

Result: nav center div contains three NavLinks in order: Calendario | Apuestas | Tabla.

No other changes to Navbar.tsx. Do not modify token balance display, sign-out button, brand text, or any other element.
  </action>
  <verify>
    <automated>grep -c "ApuestasPage" src/App.tsx && grep -c "apuestas" src/components/Navbar.tsx && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/App.tsx imports ApuestasPage and has a Route for path="/apuestas" inside the ProtectedRoute layout
    - src/components/Navbar.tsx has a NavLink to="/apuestas" with text "Apuestas" positioned between Calendario and Tabla NavLinks
    - npx tsc --noEmit passes with no errors
    - No other code in App.tsx or Navbar.tsx was changed
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end smoke test of the complete /apuestas feature</name>
  <what-built>Wave 4 has wired ApuestasPage into the app routing and added the Apuestas NavLink. The full Phase 3 feature is now live (subject to live DB from Wave 2).</what-built>
  <how-to-verify>
Run the app locally: npm run dev

Then verify each of these in the browser:

**Step 1 — Navigation**
- Open http://localhost:5173 and log in
- Confirm the Navbar shows: Calendario | Apuestas | Tabla (in that order)
- Click "Apuestas" — confirm you land on /apuestas with the page heading "Apuestas"
- Confirm the "Apuestas" link turns green (active state) when on /apuestas

**Step 2 — Pool cards load**
- Confirm the test pool seeded in Wave 2 appears in the "Activas" section
- Confirm each option shows "—" for odds (no bets yet) or a percentage if bets were placed
- Confirm the pool card has the "Abierta" green badge
- Confirm the deadline shows in your local timezone format (e.g., "Cierra: 13 de junio · 02:00 CDT")

**Step 3 — Bet placement flow**
- Click the pool card — confirm BetModal opens with the pool question in the header
- Confirm option buttons are clickable and selecting one shows a green border
- Confirm the odds show "(actual: —)" next to each option (or current %)
- Enter 5 in the fichas input — confirm the "Confirmar apuesta" button stays disabled
- Enter 50 in the fichas input — confirm the button becomes active
- Click "Confirmar apuesta" — confirm the confirmation step shows "[Opción] · 50 fichas"
- Confirm the confirmation body says "¿Estás seguro? Esta apuesta no se puede cambiar. Se descontarán 50 fichas."
- Click "Confirmar" — confirm:
  a. The Navbar token balance drops by 50 immediately
  b. The modal closes
  c. The pool card now shows a green "Tu apuesta: [Opción] · 50 fichas" badge
  d. Clicking the card again opens the read-only modal (no bet form shown)

**Step 4 — Realtime odds update (if you have a second browser session or incognito window)**
- Open /apuestas in two browser windows logged in as two different users (or skip if only one test account)
- Place a bet in window 1 — confirm odds update in window 2 without page refresh

**Step 5 — Auth guard**
- Log out
- Navigate directly to http://localhost:5173/apuestas
- Confirm you are redirected to /login

Type "approved" when all steps pass, or describe which step failed and what you observed.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 3 complete, or describe what failed for debugging.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated browser→/apuestas | Redirect handled by ProtectedRoute guard already in place |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Elevation of Privilege | /apuestas accessed without auth | mitigate | Route is inside ProtectedRoute — existing guard redirects to /login; no new auth logic needed |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; wiring only |
</threat_model>

<verification>
After both tasks complete:
1. grep -c "path=\"/apuestas\"" src/App.tsx returns 1
2. grep -c "ApuestasPage" src/App.tsx returns at least 2 (import + Route usage)
3. grep -c "to=\"/apuestas\"" src/components/Navbar.tsx returns 1
4. npx tsc --noEmit passes with no errors
5. Human has approved end-to-end smoke test
</verification>

<success_criteria>
- /apuestas route is protected by ProtectedRoute (unauthenticated → redirected to /login)
- Navbar shows three links in order: Calendario | Apuestas | Tabla
- Full bet placement flow works end-to-end: pool card → modal → two-step confirm → RPC → token balance update → badge
- Human smoke test approved
</success_criteria>

<output>
Create `.planning/phases/03-apuestas-bet-pools/03-04-SUMMARY.md` when done
</output>
