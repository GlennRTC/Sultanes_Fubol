---
phase: 03-apuestas-bet-pools
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - src/components/PoolCard.tsx
  - src/components/BetModal.tsx
  - src/pages/ApuestasPage.tsx
autonomous: true
requirements: [APU-02, APU-03, APU-04, APU-05, APU-06, APU-07, TOK-02]

must_haves:
  truths:
    - "ApuestasPage fetches bet_pools with pool_options, user's own bets, and pool_option_totals in parallel on mount"
    - "ApuestasPage subscribes to bets INSERT events via Supabase Realtime and updates odds in component state"
    - "Realtime channel is cleaned up on unmount via supabase.removeChannel()"
    - "PoolCard shows parimutuel odds as percentages, or — when no bets exist"
    - "PoolCard shows the correct status badge (Abierta/Cerrada/Resuelta) per pool status"
    - "PoolCard shows bet badge when user has bet: open/closed shows Tu apuesta, resolved win shows Ganaste, resolved loss shows Perdiste"
    - "Winning option in resolved pool is highlighted with border-green-500 and text-green-400 font-bold"
    - "BetModal has three render modes: editable two-step, read-only (user has bet), closed/resolved (no action)"
    - "BetModal step 1 shows option selector buttons with live odds and fichas amount input (min 10)"
    - "BetModal step 2 confirms the selection; on confirm calls supabase.rpc place_bet and calls updateTokens(-amount) only on success"
    - "BetModal maps all Spanish error messages for: insufficient_tokens, pool_not_open, below_minimum_bet, 23505 (duplicate)"
    - "Pool cards are divided into Activas (open + closed) and Cerradas (resolved) sections"
  artifacts:
    - path: "src/components/PoolCard.tsx"
      provides: "Single pool card with odds, status badge, bet badge, click handler"
      contains: "PoolCard"
    - path: "src/components/BetModal.tsx"
      provides: "Three-mode bet modal with two-step confirm, RPC call, error mapping"
      contains: "BetModal"
    - path: "src/pages/ApuestasPage.tsx"
      provides: "Full /apuestas page with parallel fetch, Realtime subscription, two sections"
      contains: "ApuestasPage"
  key_links:
    - from: "ApuestasPage.tsx"
      to: "supabase Realtime"
      via: "supabase.channel('bets-all').on('postgres_changes')"
      pattern: "removeChannel"
    - from: "BetModal.tsx"
      to: "place_bet RPC"
      via: "supabase.rpc('place_bet', { p_pool_id, p_option_id, p_amount })"
      pattern: "place_bet"
    - from: "BetModal.tsx"
      to: "useAuthStore"
      via: "updateTokens(-amount) called ONLY in success branch"
      pattern: "updateTokens"
---

<objective>
Build the three new frontend components: PoolCard, BetModal, and ApuestasPage. PoolCard renders a single pool with parimutuel odds, status badges, and bet badges. BetModal handles the two-step bet placement flow wired to place_bet RPC. ApuestasPage orchestrates data fetching, Realtime subscription, and renders pools in two sections.

Purpose: The user-facing feature. After this wave, the full /apuestas flow is functional (page, cards, modal, real-time odds) — wiring into App.tsx routes and Navbar happens in Wave 4.
Output: src/components/PoolCard.tsx, src/components/BetModal.tsx, src/pages/ApuestasPage.tsx
</objective>

<execution_context>
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-apuestas-bet-pools/03-CONTEXT.md
@.planning/phases/03-apuestas-bet-pools/03-RESEARCH.md
@.planning/phases/03-apuestas-bet-pools/03-UI-SPEC.md
@src/components/PredictionModal.tsx
@src/pages/CalendarPage.tsx
@src/types/index.ts
@src/store/authStore.ts
@src/lib/supabase.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build PoolCard.tsx and BetModal.tsx</name>
  <files>src/components/PoolCard.tsx, src/components/BetModal.tsx</files>
  <action>
Create src/components/PoolCard.tsx and src/components/BetModal.tsx following the patterns from MatchCard.tsx and PredictionModal.tsx respectively. All copy is in Spanish; all Tailwind classes follow 03-UI-SPEC.md exactly.

**PoolCard.tsx**

Props interface:
  interface PoolCardProps {
    pool: BetPool;                                // includes pool_options
    userBet?: Bet;                                // undefined if user has not bet
    betTotals: Record<string, number>;            // option_id -> tokens_total; used for odds display
    onOpen: (pool: BetPool) => void;              // click handler — parent manages modal state
  }

Imports: React, BetPool, PoolOption, Bet from '../types/index', formatInTimeZone from 'date-fns-tz', es from 'date-fns/locale'.

calcOdds helper function (define inside the file, not exported):
  function calcOdds(poolId: string, options: PoolOption[], betTotals: Record<string, number>): Record<string, number | null> {
    const poolTotal = options.reduce((sum, opt) => sum + (betTotals[opt.id] ?? 0), 0);
    if (poolTotal === 0) return Object.fromEntries(options.map((o) => [o.id, null]));
    return Object.fromEntries(options.map((o) => [o.id, Math.round(((betTotals[o.id] ?? 0) / poolTotal) * 100)]));
  }
  // Division-by-zero guard: returns null for all options when poolTotal === 0; display "—" in those cases (Pitfall 4)

Status badge helper:
  function statusBadge(status: 'open' | 'closed' | 'resolved'): JSX.Element — returns a span with:
    open:     className="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium text-green-400 bg-green-900/50" text "Abierta"
    closed:   className="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium text-slate-400 bg-slate-700" text "Cerrada"
    resolved: className="inline-flex items-center rounded-full px-2 py-0.5 text-xs font-medium text-slate-500 bg-slate-800" text "Resuelta"

PoolCard component structure (per 03-UI-SPEC.md §PoolCard):
1. Outer container div: role="button" tabIndex={0} onClick={() => onOpen(pool)} onKeyDown (Enter + Space triggers onOpen) className="bg-slate-800 border border-slate-700 rounded-xl p-4 mb-3 cursor-pointer hover:border-slate-500 transition-colors min-h-[80px] flex flex-col gap-2"
2. Pool question: p className="text-base text-slate-100 font-bold mb-1" — pool.question
3. Status + deadline row: div className="flex items-center gap-2 text-sm text-slate-400 mb-2"
   - statusBadge(pool.status)
   - If pool.status === 'open': span className="text-xs text-slate-400" with "Cierra: [deadline formatted in local timezone as 'd de MMMM · HH:mm zzz' using formatInTimeZone with es locale]". Use the user's detected timezone from localStorage or Intl.DateTimeFormat().resolvedOptions().timeZone as fallback — PoolCard receives no explicit timezone prop; read timezone with: const tz = localStorage.getItem('fubol_timezone') ?? Intl.DateTimeFormat().resolvedOptions().timeZone;
   - If pool.status === 'resolved' and pool.winning_option_id: span className="text-xs text-slate-500" — "Resultado: [label of winning option]" where winning option label is found by pool.pool_options?.find(o => o.id === pool.winning_option_id)?.label ?? ''
4. Options list (sorted by position ascending): div className="flex flex-col gap-1"
   For each option in pool.pool_options sorted by position:
   - const odds = oddsMap[option.id];  // oddsMap = calcOdds(...) called before render
   - const isWinning = pool.status === 'resolved' && option.id === pool.winning_option_id;
   - Row div: className="flex items-center justify-between text-sm"
     - Option label span: className={isWinning ? "text-green-400 font-bold" : "text-slate-300"} — option.label
     - Percentage span: className={isWinning ? "text-green-400 font-bold" : "text-slate-400"} — odds !== null ? `${odds}%` : "—"
5. Bet badge (render when userBet is defined):
   Determine badge content and color:
   - Pool is resolved + userBet.tokens_won !== null && userBet.tokens_won > 0: "Ganaste: +[userBet.tokens_won] fichas" className="inline-flex items-center bg-slate-700 rounded-full px-2 py-0.5 text-xs text-green-400 mt-1"
   - Pool is resolved + (userBet.tokens_won === null || userBet.tokens_won === 0): "Perdiste" className="inline-flex items-center bg-slate-700 rounded-full px-2 py-0.5 text-xs text-slate-500 mt-1"
   - Pool is open or closed: "Tu apuesta: [label of option user bet on] · [userBet.tokens_wagered] fichas" className="inline-flex items-center bg-slate-700 rounded-full px-2 py-0.5 text-xs text-green-400 mt-1" — option label found by: pool.pool_options?.find(o => o.id === userBet.option_id)?.label ?? ''

**BetModal.tsx**

Props interface:
  interface BetModalProps {
    pool: BetPool;                  // full pool with pool_options embedded
    userBet?: Bet;                  // undefined if no bet placed yet
    betTotals: Record<string, number>;  // option_id -> tokens_total for live odds display inside modal
    onClose: () => void;
    onSuccess: (bet: { optionId: string; amount: number }) => void;
  }

Imports: useState, useEffect from 'react', formatInTimeZone from 'date-fns-tz', es from 'date-fns/locale', supabase from '../lib/supabase', useAuthStore from '../store/authStore', BetPool, Bet from '../types/index'.

State:
  const [step, setStep] = useState<'input' | 'confirm'>('input');
  const [selectedOptionId, setSelectedOptionId] = useState<string>('');
  const [amount, setAmount] = useState<string>('');
  const [error, setError] = useState('');
  const [loading, setLoading] = useState(false);

Derived state:
  const isOpen = pool.status === 'open';
  const amountNum = parseInt(amount, 10);
  const canProceed = selectedOptionId !== '' && !isNaN(amountNum) && amountNum >= 10;

Escape key handler (same as PredictionModal lines 38-44):
  useEffect(() => { function handleKeyDown(e: KeyboardEvent) { if (e.key === 'Escape') onClose(); } document.addEventListener('keydown', handleKeyDown); return () => document.removeEventListener('keydown', handleKeyDown); }, [onClose]);

handleConfirm async function:
  1. setLoading(true); setError('');
  2. const { error: rpcError } = await supabase.rpc('place_bet', { p_pool_id: pool.id, p_option_id: selectedOptionId, p_amount: amountNum });
  3. If rpcError: map error codes to Spanish messages (follow PredictionModal pattern exactly):
     - includes('insufficient_tokens') → 'No tienes suficientes fichas para esta apuesta.'
     - includes('pool_not_open') OR includes('pool_deadline_passed') → 'Esta apuesta ya no acepta fichas.'
     - includes('below_minimum_bet') → 'El mínimo es 10 fichas.'
     - code === '23505' → 'Ya tienes una apuesta en este pool.'
     - otherwise → 'No se pudo registrar tu apuesta. Intenta de nuevo.'
     Then: setStep('input'); setLoading(false);
  4. If no error: useAuthStore.getState().updateTokens(-amountNum); — ONLY in success branch (Pitfall 5)
     onSuccess({ optionId: selectedOptionId, amount: amountNum });
     onClose();

Timezone: const tz = localStorage.getItem('fubol_timezone') ?? Intl.DateTimeFormat().resolvedOptions().timeZone;

Heading id: const headingId = 'bet-modal-' + pool.id;

Render structure (per 03-UI-SPEC.md §BetModal):

Overlay: div className="fixed inset-0 bg-black/60 flex items-center justify-center z-50 px-4"
Panel: div role="dialog" aria-modal="true" aria-labelledby={headingId} className="relative bg-slate-800 border border-slate-700 rounded-xl p-6 w-full max-w-[480px] mx-auto"

Always rendered inside panel:
  Close button (× per UI-SPEC; Unicode U+00D7 not ASCII X): button type="button" onClick={onClose} aria-label="Cerrar" className="absolute top-4 right-4 text-slate-400 hover:text-slate-100 text-xl leading-none min-h-[44px] min-w-[44px] flex items-center justify-center" — content: ×
  Modal header: h2 id={headingId} className="text-xl font-bold text-slate-100 mb-1 pr-10" — pool.question
  Deadline (open pools): p className="text-sm text-slate-400 mb-4" — "Cierra: [formatInTimeZone(new Date(pool.deadline), tz, "d 'de' MMMM · HH:mm zzz", { locale: es })]"
  Error banner when error: div role="alert" className="bg-red-900/50 border border-red-700 rounded-lg px-4 py-3 mb-4" — p className="text-sm text-red-300" — {error}

Three render modes (mutually exclusive):

MODE A — Read-only (userBet is defined):
  Show pool options list with the user's selected option highlighted with border-green-500.
  "Apostaste: [userBet.tokens_wagered] fichas" in p className="text-sm text-slate-400 mt-2 mb-4"
  If pool is resolved and userBet.tokens_won > 0: p className="text-sm text-green-400 font-bold mb-4" — "Ganaste: +[userBet.tokens_won] fichas"
  If pool is resolved and (tokens_won === null || tokens_won === 0): p className="text-sm text-slate-500 mb-4" — "Perdiste"
  Dismiss button: button type="button" onClick={onClose} className="text-sm text-slate-400 hover:text-slate-300 underline text-center block w-full" — "Cerrar"
  No CTA button.

MODE B — Closed/resolved, no bet placed (!userBet && pool.status !== 'open'):
  p className="text-sm text-slate-400 mb-4" — "Esta apuesta ya no acepta fichas."
  If pool.status === 'resolved': show pool options in read-only list; winning option row gets border-green-500 left border and text-green-400 font-bold labels.
  Dismiss button: "Cerrar"

MODE C — Editable two-step (!userBet && pool.status === 'open'):

  STEP INPUT:
  Options list container: div className="flex flex-col gap-2 mb-4"
    For each option (sorted by position), render:
    button type="button" onClick={() => setSelectedOptionId(option.id)}
      className={selectedOptionId === option.id
        ? "w-full text-left px-4 py-3 rounded-lg bg-slate-700 border border-green-500 text-sm text-slate-100 font-bold min-h-[44px] transition-colors"
        : "w-full text-left px-4 py-3 rounded-lg bg-slate-700 border border-slate-600 text-sm text-slate-300 hover:border-slate-500 min-h-[44px] transition-colors"
      }
      Inside: span for label + span className="text-xs text-slate-400 ml-2" for odds: "(actual: [N]%)" or "(actual: —)" when null. Use same calcOdds pattern from PoolCard (compute inline here from betTotals prop).

  Token amount section: div className="flex flex-col mb-4"
    label htmlFor="bet-amount" className="text-sm text-slate-300 mb-1" — "¿Cuántas fichas apuestas?"
    input id="bet-amount" type="number" min="10" step="1" value={amount} onChange={(e) => setAmount(e.target.value)}
      className="w-24 bg-slate-700 border border-slate-600 rounded-lg px-3 py-2 text-center text-base text-slate-100 focus:outline-none focus:border-green-500 focus:ring-1 focus:ring-green-500"
    p className="text-xs text-slate-400 mt-1" — "Mín. 10 fichas"

  CTA button: button type="button" disabled={!canProceed} onClick={() => setStep('confirm')}
    className="w-full bg-green-500 hover:bg-green-600 active:bg-green-700 disabled:opacity-50 disabled:cursor-not-allowed text-white font-bold text-base px-6 py-3 rounded-lg"
    text: "Confirmar apuesta"

  STEP CONFIRM:
  Summary line: p className="text-base text-slate-100 mb-2" — "[label of selectedOptionId option] · [amountNum] fichas"
    (find label via: pool.pool_options?.find(o => o.id === selectedOptionId)?.label ?? '')
  Body: p className="text-sm text-slate-400 mb-4" — "¿Estás seguro? Esta apuesta no se puede cambiar. Se descontarán [amountNum] fichas."
  Confirm button: button type="button" disabled={loading} aria-busy={loading} aria-disabled={loading} onClick={handleConfirm}
    className="w-full bg-green-500 hover:bg-green-600 active:bg-green-700 disabled:opacity-50 disabled:cursor-not-allowed text-white font-bold text-base px-6 py-3 rounded-lg mb-2"
    Loading state: span with inline-block w-4 h-4 rounded-full border-2 border-white border-t-transparent animate-spin (same spinner as PredictionModal)
    Non-loading text: "Confirmar"
  Cancel link: button type="button" onClick={() => setStep('input')} className="text-sm text-slate-400 hover:text-slate-300 underline text-center block w-full" — "Cancelar"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/components/PoolCard.tsx exists and exports PoolCard
    - src/components/BetModal.tsx exists and exports BetModal
    - PoolCard correctly renders status badge, options with odds, and bet badge for all pool states
    - BetModal implements all three render modes with correct copy and styles
    - BetModal calls updateTokens only in the success branch (Pitfall 5 avoided)
    - Both files pass TypeScript compilation with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Build ApuestasPage.tsx</name>
  <files>src/pages/ApuestasPage.tsx</files>
  <action>
Create src/pages/ApuestasPage.tsx following the structure of CalendarPage.tsx. All component state (no new Zustand store). Mirrors the fetch + Realtime subscription pattern from RESEARCH.md Pattern 3.

Imports: useState, useEffect from 'react', supabase from '../lib/supabase', useAuthStore from '../store/authStore', PoolCard from '../components/PoolCard', BetModal from '../components/BetModal', FullScreenSpinner from '../components/FullScreenSpinner', BetPool, Bet, PoolOptionTotal from '../types/index'.

State:
  const [pools, setPools] = useState<BetPool[]>([]);
  const [bets, setBets] = useState<Bet[]>([]);
  const [betTotals, setBetTotals] = useState<Record<string, Record<string, number>>>({});
    // Shape: { [pool_id]: { [option_id]: tokens_total } }
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState('');
  const [selectedPool, setSelectedPool] = useState<BetPool | null>(null);

  const { user } = useAuthStore();

**Initial data fetch (parallel, mirrors CalendarPage lines 54-77):**
useEffect that calls an async fetchData function:
  1. setLoading(true)
  2. const [poolResult, betResult, totalsResult] = await Promise.all([
       supabase.from('bet_pools').select('*, pool_options(*)').order('deadline'),
       supabase.from('bets').select('*').eq('user_id', user!.id),
       supabase.from('pool_option_totals').select('*'),
     ])
  3. If poolResult.error: setError('No se pudieron cargar las apuestas. Recarga la página.'); setLoading(false); return;
  4. setPools(poolResult.data ?? [])
  5. If betResult.error: console.warn('bets fetch failed:', betResult.error.message)
  6. setBets(betResult.data ?? [])
  7. Build initial betTotals map from totalsResult.data (type PoolOptionTotal[]):
     const initialTotals: Record<string, Record<string, number>> = {};
     for (const row of totalsResult.data ?? []) {
       if (!initialTotals[row.pool_id]) initialTotals[row.pool_id] = {};
       initialTotals[row.pool_id][row.option_id] = row.tokens_total;
     }
     setBetTotals(initialTotals);
  8. setLoading(false)

**Realtime subscription (RESEARCH.md Pattern 3, Pitfall 1 + 7):**
Second useEffect (separate from fetch to control cleanup lifecycle):
  const channel = supabase
    .channel('bets-all')
    .on(
      'postgres_changes',
      { event: 'INSERT', schema: 'public', table: 'bets' },
      (payload) => {
        const newBet = payload.new as { pool_id: string; option_id: string; tokens_wagered: number };
        setBetTotals((prev) => {
          const updated = { ...prev };
          const poolTotals = { ...(updated[newBet.pool_id] ?? {}) };
          poolTotals[newBet.option_id] = (poolTotals[newBet.option_id] ?? 0) + newBet.tokens_wagered;
          updated[newBet.pool_id] = poolTotals;
          return updated;
        });
      }
    )
    .subscribe();
  return () => { supabase.removeChannel(channel); };  // Pitfall 7: cleanup on unmount

**handleBetSuccess function (optimistic badge update — RESEARCH.md Pattern 5):**
  function handleBetSuccess({ optionId, amount }: { optionId: string; amount: number }) {
    if (!selectedPool) return;
    const newBet: Bet = {
      id: 'local-' + Date.now(),
      user_id: user!.id,
      pool_id: selectedPool.id,
      option_id: optionId,
      tokens_wagered: amount,
      tokens_won: null,
      created_at: new Date().toISOString(),
    };
    setBets((prev) => [...prev, newBet]);
    // updateTokens is called inside BetModal.handleConfirm on success (not here — avoids double call)
  }

**Helper: getBetForPool**
  function getBetForPool(poolId: string): Bet | undefined {
    return bets.find((b) => b.pool_id === poolId);
  }

**Helper: getFlatTotals(poolId: string): Record<string, number>**
  Returns betTotals[poolId] ?? {} — the option_id->tokens_total map for a single pool.

**Render structure (per 03-UI-SPEC.md §ApuestasPage):**
Page wrapper: div className="min-h-[calc(100vh-56px)] bg-slate-900 px-4 py-6"
  h1 className="text-xl font-bold text-slate-100 mb-4" — "Apuestas"
  If loading: return FullScreenSpinner
  If error: div role="alert" className="bg-red-900/50 border border-red-700 rounded-lg px-4 py-3 mb-4" — p className="text-sm text-red-300" — {error}

  Derived arrays (computed before render, not in state):
    const activePools = pools.filter(p => p.status === 'open' || p.status === 'closed');
    const closedPools = pools.filter(p => p.status === 'resolved');
    // "Activas" includes open AND closed; "Cerradas" includes resolved (per D-09)

  ACTIVAS SECTION:
    If activePools.length === 0: show empty state
      p className="text-base text-slate-400 font-bold mb-1" — "Sin apuestas activas"
      p className="text-sm text-slate-400" — "El administrador no ha creado ninguna apuesta abierta todavía."
    Else:
      h2 className="text-sm font-bold text-slate-300 mt-2 mb-3 uppercase tracking-wide" — "Activas"
      Map activePools (sorted by deadline ascending — they come from DB ordered by deadline):
        PoolCard pool={pool} userBet={getBetForPool(pool.id)} betTotals={getFlatTotals(pool.id)} onOpen={setSelectedPool}

  CERRADAS SECTION (only render if closedPools.length > 0 — per UI-SPEC: "section is simply hidden if empty"):
    h2 className="text-sm font-bold text-slate-300 mt-6 mb-3 uppercase tracking-wide" — "Cerradas"
    Map closedPools (most recently resolved first — sort by created_at desc or just reverse order since DB returns by deadline):
      PoolCard pool={pool} userBet={getBetForPool(pool.id)} betTotals={getFlatTotals(pool.id)} onOpen={setSelectedPool}

  MODAL (render when selectedPool is not null):
    BetModal
      pool={selectedPool}
      userBet={getBetForPool(selectedPool.id)}
      betTotals={getFlatTotals(selectedPool.id)}
      onClose={() => setSelectedPool(null)}
      onSuccess={handleBetSuccess}
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/pages/ApuestasPage.tsx exists and exports ApuestasPage
    - Parallel fetch of bet_pools, bets, pool_option_totals with correct error handling
    - Realtime subscription on bets INSERT with cleanup in useEffect return
    - handleBetSuccess injects synthetic Bet into local state (optimistic badge update)
    - Two sections: Activas (open+closed) and Cerradas (resolved)
    - Empty state rendered when no open pools exist
    - Cerradas section hidden when no resolved pools exist
    - TypeScript compiles cleanly with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend→place_bet RPC | p_pool_id, p_option_id, p_amount from user input; DB function validates all three |
| Realtime payload→frontend state | payload.new is cast to expected shape; malformed payloads handled by optional chaining |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | updateTokens called on RPC error path | mitigate | updateTokens(-amount) is called only inside the success branch of handleConfirm, after confirming error === null (Pitfall 5 pattern from PredictionModal) |
| T-03-11 | Tampering | Realtime subscription not cleaned up | mitigate | useEffect returns () => supabase.removeChannel(channel) — mandatory cleanup (Pitfall 7) |
| T-03-12 | Information Disclosure | betTotals state exposes individual bet amounts | accept | betTotals contains aggregated totals only (from pool_option_totals view), not individual user bet rows; Realtime events show option_id + tokens_wagered of each insert (public by design, D-04) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages; zero new dependencies confirmed in RESEARCH.md |
</threat_model>

<verification>
After both tasks complete:
1. npx tsc --noEmit passes with no errors (all three new files)
2. grep -c "removeChannel" src/pages/ApuestasPage.tsx returns at least 1
3. grep -c "place_bet" src/components/BetModal.tsx returns at least 1
4. grep -c "updateTokens" src/components/BetModal.tsx returns at least 1 AND it appears after "if (!rpcError)" (success branch only)
5. grep -c "pool_option_totals" src/pages/ApuestasPage.tsx returns at least 1
6. All three files exist: src/components/PoolCard.tsx, src/components/BetModal.tsx, src/pages/ApuestasPage.tsx
</verification>

<success_criteria>
- PoolCard renders correctly for all five pool/bet state combinations (open no-bet, open with-bet, closed with-bet, resolved won, resolved lost)
- BetModal implements all three modes: editable two-step, read-only, closed notice
- BetModal calls supabase.rpc('place_bet') on confirm and maps all error codes to Spanish messages
- ApuestasPage fetches data in parallel, subscribes to Realtime bets INSERT, and cleans up the channel on unmount
- TypeScript compiles with no errors across all three new files
</success_criteria>

<output>
Create `.planning/phases/03-apuestas-bet-pools/03-03-SUMMARY.md` when done
</output>
