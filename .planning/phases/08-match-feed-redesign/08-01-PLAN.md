---
phase: 08-match-feed-redesign
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/FinishedMatchRow.tsx
  - src/pages/CalendarPage.tsx
autonomous: true
requirements:
  - live-first-feed-ordering
  - compact-finished-rows
  - remove-finished-accordion

must_haves:
  truths:
    - "On /calendario, live matches render at the top in their existing full-size glowing cards"
    - "Scheduled matches render next, in chronological order, in their existing full-size cards"
    - "Finished matches render at the bottom, always visible (no toggle/accordion), in a new compact single-line row format"
    - "Tapping any finished match row still opens PredictionModal showing the prediction vs. final score, identical behavior to before"
    - "The same live -> scheduled -> finished ordering applies inside the Por grupo view's active group"
    - "The Grupo/Equipo filter bar still works -- filtering happens before the status split"
    - "The Phase 7 Partidos finalizados toggle/accordion code is fully removed (no dead code left behind)"
  artifacts:
    - path: "src/components/FinishedMatchRow.tsx"
      provides: "Compact single-line finished-match row component, drop-in prop-compatible with MatchCard"
      contains: "FinishedMatchRowProps"
    - path: "src/pages/CalendarPage.tsx"
      provides: "Status-ordered feed (live -> scheduled -> finished) replacing date-grouped + accordion rendering, for both Por fecha and Por grupo views"
      contains: "FinishedMatchRow"
  key_links:
    - from: "src/pages/CalendarPage.tsx"
      to: "src/components/FinishedMatchRow.tsx"
      via: "import and render for finished-status matches in both views"
      pattern: "finished\\.map\\(.*FinishedMatchRow"
    - from: "src/components/FinishedMatchRow.tsx"
      to: "src/lib/countryCode.ts"
      via: "getCountryCode import for flag rendering"
      pattern: "getCountryCode"
    - from: "src/pages/CalendarPage.tsx"
      to: "src/components/PredictionModal.tsx"
      via: "onCardClick handler (setSelectedMatch) shared by live/scheduled MatchCards and finished FinishedMatchRows"
      pattern: "onCardClick=\\{setSelectedMatch\\}"
---

<objective>
Replace CalendarPage's date-grouped match list with a status-ordered feed — live matches first (full MatchCard, existing glow), scheduled next (chronological, full MatchCard), finished last (new compact FinishedMatchRow, always visible, no toggle) — and remove Phase 7's now-obsolete finished-match accordion entirely. Applies to both "Por fecha" and "Por grupo" views.

Purpose: Per CONTEXT.md (08-match-feed-redesign) locked decisions 1-4 — date is no longer the primary grouping mechanism; status is. Finished matches no longer need an expand/collapse interaction since their compact format already declutters the feed by size, not by hiding.
Output: New src/components/FinishedMatchRow.tsx; rewritten match-rendering sections of src/pages/CalendarPage.tsx for both views, with all Phase 7 accordion state/UI removed.
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/phases/08-match-feed-redesign/CONTEXT.md
@/home/glenn/Denv/Sultanes_Fubol/src/pages/CalendarPage.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/components/MatchCard.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/lib/countryCode.ts
@/home/glenn/Denv/Sultanes_Fubol/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FinishedMatchRow.tsx -- compact single-line finished-match component</name>
  <files>src/components/FinishedMatchRow.tsx</files>
  <action>
Create a new sibling component to MatchCard.tsx. Define interface FinishedMatchRowProps with the same shape as MatchCardProps: match: Match, prediction?: Prediction, timezone: string, onCardClick: (m: Match) => void -- import Match and Prediction from ../types/index. Keep timezone in the props interface for drop-in shape-compatibility with MatchCard, but the compact row does not render a separate date/time string (locked decision 3: content is flag/team/score/team/flag only) -- if it is not used in the render body, simply omit it from the destructured parameters so no unused-variable warning is produced.

Reuse getCountryCode from ../lib/countryCode for flags -- same import path and usage pattern as MatchCard.tsx (a span with className like fi fi-{code} and aria-hidden="true").

Render a single root element with role="button", tabIndex={0}, an onClick handler that calls onCardClick(match), and an onKeyDown handler that calls onCardClick(match) when the key is Enter or Space -- identical wiring pattern to MatchCard.tsx so PredictionModal opens the same way on tap/click/keyboard. Root className: zinc/emerald palette, reduced padding relative to MatchCard's px-4 py-3 -- use px-3 py-2, bg-zinc-900, border border-zinc-700, rounded-lg, mb-1.5, cursor-pointer, hover:border-zinc-500, transition-colors, flex items-center. Do NOT apply the .live-glow class here -- this component only ever renders finished matches.

Inside the root, use a 3-column grid for the matchup line, e.g. grid grid-cols-[1fr_44px_1fr] items-center gap-1.5 w-full (tighter than MatchCard's grid-cols-[1fr_68px_1fr], since there is no separate meta row to balance against). Left column: flag (if getCountryCode(match.home_team) resolves, rendered with text-sm rounded-sm shrink-0) plus home team name, right-aligned content, name truncated (text-xs font-semibold text-zinc-200 truncate), matching MatchCard's "flag then name, justify-end" convention for the home side. Center column: score, text-sm font-bold text-zinc-100 tabular-nums text-center, formatted as match.home_score, an en dash, match.away_score (matching MatchCard's exact score format -- finished matches always have non-null scores so no fallback needed). Right column: away team name (truncated, text-xs font-semibold text-zinc-200 truncate) plus flag, left-aligned content, mirroring MatchCard's "name then flag, justify-start" convention for the away side.

If prediction is truthy, render one small emerald dot indicator inside the root (after the grid, a span with classes like w-1.5 h-1.5 rounded-full bg-emerald-400 shrink-0 ml-2 and aria-hidden="true") to flag that the user predicted this match -- keep it minimal, no larger than the row's text, no prediction score text shown inline (that level of detail is reserved for the modal).

No venue row, no status badge text, no meta row -- locked decision 3 explicitly excludes these; the row's position in the feed (always last) already implies "finished".
  </action>
  <verify>
    <automated>grep -c "FinishedMatchRowProps" src/components/FinishedMatchRow.tsx; grep -c "getCountryCode" src/components/FinishedMatchRow.tsx; grep -c "onCardClick(match)" src/components/FinishedMatchRow.tsx</automated>
  </verify>
  <done>
    - src/components/FinishedMatchRow.tsx exists, exports FinishedMatchRow with FinishedMatchRowProps matching MatchCardProps shape (match, prediction?, timezone, onCardClick)
    - Renders a single-line row: flag, home team, score, away team, flag -- no venue, no status badge, no meta row
    - onClick/onKeyDown call onCardClick(match) identically to MatchCard.tsx
    - Reuses getCountryCode from src/lib/countryCode.ts
    - Zinc/emerald palette only, no new colors
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite CalendarPage.tsx to a status-ordered feed and remove the Phase 7 accordion</name>
  <files>src/pages/CalendarPage.tsx</files>
  <action>
Import FinishedMatchRow from ../components/FinishedMatchRow at the top of the file alongside the existing MatchCard import.

Remove the Phase 7 accordion state and helper entirely: delete the openFinished useState declaration and the toggleFinished function. Search the whole file for openFinished and toggleFinished and remove every reference, including the toggle button and chevron svg blocks in both the Por fecha and Por grupo render paths -- leave zero dead code.

groupMatchesByLocalDate is no longer used to build section headers (locked decision 1) -- remove the function definition and its only call site in the Por fecha view's render path. formatDateHeader becomes unused once the date headers are removed -- remove it too unless you find another live call site (there should not be one; confirm via grep before deleting). Do not touch detectTimezone, saveTimezone, SUPPORTED_TIMEZONES, or the TimezonePicker component usage -- those are unrelated to date-grouping and stay as-is, since displayTime inside MatchCard remains the only per-match date indicator now that there are no section headers.

Por fecha view: after the existing filter bar (filteredMatches computed from filterGroup/filterTeam, unchanged), replace the entire date-grouped rendering block with three derived arrays computed directly from filteredMatches, exactly as specified in CONTEXT.md's technical notes: live is filteredMatches filtered to status === 'live'; scheduled is filteredMatches filtered to status === 'scheduled', sorted ascending by match_datetime (earliest first); finished is filteredMatches filtered to status === 'finished', sorted descending by match_datetime (most recently finished first). Keep the existing "Sin partidos" empty state, but trigger it when filteredMatches.length === 0 (not when a per-date-group list is empty). Render order: map live matches to MatchCard (same props as today: match, prediction via getPredictionForMatch(m.id), timezone, onCardClick={setSelectedMatch}), then map scheduled matches to MatchCard with identical props, then -- only when finished.length > 0 -- render a small non-interactive label such as a paragraph reading "Finalizados" styled text-xs font-bold text-zinc-500 uppercase tracking-wide mt-4 mb-2 (NOT a button, no chevron, no aria-expanded, no onClick) followed by mapping finished matches to FinishedMatchRow with the same props (match, prediction, timezone, onCardClick={setSelectedMatch}).

Por grupo view: apply the identical three-way split to the active group's matches instead of filteredMatches: compute groupMatches as matches filtered to group_name === activeGroup, then derive live/scheduled/finished from groupMatches using the exact same filter+sort logic as the Por fecha view above. Render in the same order: live MatchCards, then scheduled MatchCards, then (if any) the "Finalizados" label and FinishedMatchRow list. Remove the old liveOrScheduled/finishedMatches/accordion-toggle block entirely and replace it with this three-way split. The group tab row (the GROUPS array mapped to buttons) and activeGroup state are unchanged.

Do not change the fetch logic (the useEffect data loading), the filter bar selects, getPredictionForMatch, handlePredictionSuccess, the TimezonePicker/TodayMatchesWidget usage, or the PredictionModal wiring at the bottom of the component -- only the match-list derivation and rendering inside each view change.
  </action>
  <verify>
    <automated>cd /home/glenn/Denv/Sultanes_Fubol && npx tsc --noEmit && grep -c "openFinished" src/pages/CalendarPage.tsx | grep -qx 0 && grep -c "toggleFinished" src/pages/CalendarPage.tsx | grep -qx 0 && grep -c "groupMatchesByLocalDate" src/pages/CalendarPage.tsx | grep -qx 0 && echo PASS</automated>
  </verify>
  <done>
    - CalendarPage.tsx imports and renders FinishedMatchRow for finished matches in both Por fecha and Por grupo views
    - Both views render in live -> scheduled -> finished order, with finished matches under a static "Finalizados" label (no toggle, no button, no chevron, no aria-expanded)
    - openFinished, toggleFinished, and groupMatchesByLocalDate are fully removed from the file (grep count is 0 for each)
    - The Grupo/Equipo filter bar still drives filteredMatches before the status split (Por fecha view)
    - Tapping a finished row or a live/scheduled card calls setSelectedMatch and opens PredictionModal identically to before
    - npx tsc --noEmit passes with no new TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source -> editor | Pure UI/rendering reorganization (new component, derived-array filter/sort, removed dead state); no new data flow, no new auth surface, no new external input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | FinishedMatchRow.tsx / CalendarPage.tsx derived arrays | accept | Cosmetic and client-side-derived-state-only change; no server interaction, all inputs already validated server-side (matches table is read-only from this page) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan -- frontend-only, no new dependencies |

</threat_model>

<verification>
After both tasks:
- src/components/FinishedMatchRow.tsx exists, exports a component with MatchCard-compatible props, renders a single-line flag/team/score/team/flag row
- src/pages/CalendarPage.tsx renders live matches first (full MatchCard, live-glow intact), then scheduled (chronological, full MatchCard), then finished (compact FinishedMatchRow, always visible) in both Por fecha and Por grupo views
- All Phase 7 accordion code (openFinished, toggleFinished, toggle button, chevron svg) is removed -- grep confirms zero matches
- groupMatchesByLocalDate and formatDateHeader are removed (no longer used as section headers)
- Tapping any match (live, scheduled, or finished) still opens PredictionModal via setSelectedMatch
- Grupo/Equipo filter bar still filters filteredMatches before the status split
- npx tsc --noEmit exits 0
</verification>

<success_criteria>
CalendarPage presents a single status-ordered feed (live -> scheduled -> finished) in both views, with finished matches always visible in a new compact row format and zero leftover accordion code from Phase 7.
</success_criteria>

<output>
Create .planning/phases/08-match-feed-redesign/08-01-SUMMARY.md when done
</output>
