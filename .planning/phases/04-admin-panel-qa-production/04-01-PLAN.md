---
phase: 04-admin-panel-qa-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0007_admin_panel.sql
  - src/types/index.ts
autonomous: true
requirements: [ADM-01, ADM-02, ADM-03, ADM-04, ADM-05, ADM-06, ADM-07, ADM-08, TOK-01, INF-03]

must_haves:
  truths:
    - "Migration 0007_admin_panel.sql exists and creates the private schema + private.is_admin() helper"
    - "admin_logs table exists with RLS enabled, admin-only SELECT policy, and NO INSERT policy"
    - "profiles_select_admin RLS policy exists as a second SELECT policy that OR-adds admin access without replacing profiles_select_own"
    - "token_transactions has a new admin-only SELECT policy"
    - "All five admin functions exist: admin_block_user, admin_grant_tokens, admin_set_match_result, create_bet_pool, admin_resolve_pool"
    - "Every admin function has REVOKE from public/anon and GRANT to authenticated in the migration"
    - "Every admin function raises 'not_admin' exception when called by a non-admin authenticated user"
    - "admin_logs table is defined BEFORE the function definitions that INSERT into it"
    - "src/types/index.ts exports AdminLog interface with all required fields"
    - "npx tsc --noEmit passes with no errors"
  artifacts:
    - path: "supabase/migrations/0007_admin_panel.sql"
      provides: "All Phase 4 DB schema: private schema, is_admin helper, admin_logs, RLS policies, 5 admin functions"
      contains: "admin_logs"
    - path: "src/types/index.ts"
      provides: "AdminLog TypeScript interface"
      contains: "AdminLog"
  key_links:
    - from: "supabase/migrations/0007_admin_panel.sql"
      to: "private.is_admin()"
      via: "CREATE SCHEMA IF NOT EXISTS private + SECURITY DEFINER function"
      pattern: "private.is_admin"
    - from: "admin functions"
      to: "admin_logs table"
      via: "INSERT inside each SECURITY DEFINER function"
      pattern: "insert into public.admin_logs"
    - from: "RLS policies on profiles + token_transactions + admin_logs"
      to: "private.is_admin()"
      via: "(select private.is_admin()) in USING clause"
      pattern: "select private.is_admin"
---

<objective>
Write migration 0007_admin_panel.sql — the complete Phase 4 DB layer: private schema, private.is_admin() SECURITY DEFINER helper, admin_logs table with RLS, two new RLS policies on existing tables, and five new SECURITY DEFINER admin wrapper functions with REVOKE/GRANT. Extend src/types/index.ts with the AdminLog interface.

Purpose: Establishes the security-correct DB contract for all admin operations. Every subsequent wave depends on this migration being applied to the live DB before any admin UI can work.
Output: supabase/migrations/0007_admin_panel.sql (ready to push), src/types/index.ts (extended with AdminLog).
</objective>

<execution_context>
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-admin-panel-qa-production/RESEARCH.md
@supabase/migrations/0004_bet_pools.sql
@supabase/migrations/0005_bet_pools_security_fixes.sql
@src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 0007_admin_panel.sql</name>
  <files>supabase/migrations/0007_admin_panel.sql</files>
  <action>
Create supabase/migrations/0007_admin_panel.sql following the comment and structure conventions from 0004_bet_pools.sql and 0005_bet_pools_security_fixes.sql. The file must contain these sections IN ORDER — section ordering is load-order critical (admin_logs must exist before functions that INSERT into it).

SECTION 1 — Private schema + is_admin() helper (RESEARCH.md Q3):

    create schema if not exists private;

    create or replace function private.is_admin()
    returns boolean
    language plpgsql
    security definer
    set search_path = public
    stable
    as $$
    begin
      return exists (
        select 1 from public.profiles
        where id = (select auth.uid())
          and is_admin = true
      );
    end;
    $$;
    -- No GRANT needed: private schema functions are not exposed by PostgREST.
    -- Called via (select private.is_admin()) in RLS policies for per-statement caching.

SECTION 2 — admin_logs table (RESEARCH.md Q4):
Create the table BEFORE the function definitions that INSERT into it.

    create table public.admin_logs (
      id              uuid        primary key default gen_random_uuid(),
      admin_id        uuid        not null references auth.users(id),
      action          text        not null,
      target_user_id  uuid        references auth.users(id),
      details         jsonb       not null default '{}',
      created_at      timestamptz not null default now()
    );

    alter table public.admin_logs enable row level security;

    -- Admin-only SELECT policy (uses SELECT-wrapped helper for per-statement caching)
    create policy "admin_logs_select_admin"
      on public.admin_logs for select
      using ((select private.is_admin()));

    -- NO INSERT policy: all inserts happen inside SECURITY DEFINER functions only.
    -- This prevents a malicious client from spoofing fake audit entries.

SECTION 3 — New RLS policies on existing tables (RESEARCH.md Q3):

    -- Second SELECT policy on profiles: OR-adds admin access.
    -- Does NOT replace the existing profiles_select_own policy.
    -- Policies are OR'd: a row is visible if ANY policy permits it.
    -- CRITICAL: use (select private.is_admin()) not private.is_admin() — the SELECT wrapper
    -- triggers Postgres initPlan optimization so the function runs once per statement,
    -- not once per row (prevents catastrophic N+1 per-row evaluation on large tables).
    create policy "profiles_select_admin"
      on public.profiles for select
      using ((select private.is_admin()));

    -- Admin-only SELECT on token_transactions (needed for AdminReportsPage: token circulation).
    -- Previously there was no SELECT policy — only service-role access.
    -- Pitfall: SELECT count(*) FROM token_transactions returns 0 rows for non-admins (RLS blocks).
    create policy "token_transactions_select_admin"
      on public.token_transactions for select
      using ((select private.is_admin()));

SECTION 4 — admin_block_user function (ADM-01, ADM-08):

    create or replace function public.admin_block_user(
      p_target_user_id uuid,
      p_blocked         boolean
    )
    returns void
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_admin_id uuid := auth.uid();
    begin
      if v_admin_id is null then
        raise exception 'not_authenticated';
      end if;
      if not exists (
        select 1 from public.profiles where id = v_admin_id and is_admin = true
      ) then
        raise exception 'not_admin';
      end if;
      if not exists (
        select 1 from public.profiles where id = p_target_user_id
      ) then
        raise exception 'user_not_found';
      end if;
      update public.profiles
        set is_blocked = p_blocked, updated_at = now()
        where id = p_target_user_id;
      insert into public.admin_logs (admin_id, action, target_user_id, details)
      values (
        v_admin_id,
        case when p_blocked then 'user_blocked' else 'user_unblocked' end,
        p_target_user_id,
        jsonb_build_object('blocked', p_blocked)
      );
    end;
    $$;

    revoke execute on function public.admin_block_user(uuid, boolean) from public, anon;
    grant execute on function public.admin_block_user(uuid, boolean) to authenticated;

SECTION 5 — admin_grant_tokens function (ADM-03, TOK-01, ADM-08):
A negative p_amount removes tokens; a positive p_amount grants them. Include a guard that
prevents the balance from going below 0 (mirrors the existing CHECK on profiles.tokens).

    create or replace function public.admin_grant_tokens(
      p_target_user_id uuid,
      p_amount          integer,
      p_note            text default ''
    )
    returns void
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_admin_id     uuid    := auth.uid();
      v_current_bal  integer;
    begin
      if v_admin_id is null then
        raise exception 'not_authenticated';
      end if;
      if not exists (
        select 1 from public.profiles where id = v_admin_id and is_admin = true
      ) then
        raise exception 'not_admin';
      end if;
      if p_amount = 0 then
        raise exception 'amount_cannot_be_zero';
      end if;
      select tokens into v_current_bal
        from public.profiles
        where id = p_target_user_id
        for update;
      if not found then
        raise exception 'user_not_found';
      end if;
      if v_current_bal + p_amount < 0 then
        raise exception 'insufficient_tokens';
      end if;
      update public.profiles
        set tokens = tokens + p_amount, updated_at = now()
        where id = p_target_user_id;
      insert into public.token_transactions (user_id, amount, type, reference_id)
      values (p_target_user_id, p_amount, 'admin_grant', null);
      insert into public.admin_logs (admin_id, action, target_user_id, details)
      values (
        v_admin_id,
        case when p_amount > 0 then 'tokens_granted' else 'tokens_removed' end,
        p_target_user_id,
        jsonb_build_object('amount', p_amount, 'note', p_note)
      );
    end;
    $$;

    revoke execute on function public.admin_grant_tokens(uuid, integer, text) from public, anon;
    grant execute on function public.admin_grant_tokens(uuid, integer, text) to authenticated;

SECTION 6 — admin_set_match_result function (ADM-04, ADM-08):
This function calls the existing calculate_prediction_points function internally. Because
admin_set_match_result is SECURITY DEFINER and runs as the function owner (postgres),
it can call calculate_prediction_points even though it is REVOKE'd from authenticated.
Pitfall 8: do NOT add a separate UPDATE matches statement — calculate_prediction_points
already does that UPDATE. Call the inner function directly.

    create or replace function public.admin_set_match_result(
      p_match_id   uuid,
      p_home_score integer,
      p_away_score integer
    )
    returns void
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_admin_id uuid := auth.uid();
    begin
      if v_admin_id is null then
        raise exception 'not_authenticated';
      end if;
      if not exists (
        select 1 from public.profiles where id = v_admin_id and is_admin = true
      ) then
        raise exception 'not_admin';
      end if;
      if p_home_score < 0 or p_away_score < 0 then
        raise exception 'invalid_score';
      end if;
      if not exists (
        select 1 from public.matches where id = p_match_id
      ) then
        raise exception 'match_not_found';
      end if;
      -- Delegate to existing calculate_prediction_points which updates the match row
      -- AND distributes points/tokens. Running as SECURITY DEFINER owner bypasses the
      -- REVOKE from authenticated on the inner function.
      perform public.calculate_prediction_points(p_match_id, p_home_score, p_away_score);
      insert into public.admin_logs (admin_id, action, target_user_id, details)
      values (
        v_admin_id,
        'match_result_entered',
        null,
        jsonb_build_object(
          'match_id', p_match_id,
          'home_score', p_home_score,
          'away_score', p_away_score
        )
      );
    end;
    $$;

    revoke execute on function public.admin_set_match_result(uuid, integer, integer) from public, anon;
    grant execute on function public.admin_set_match_result(uuid, integer, integer) to authenticated;

SECTION 7 — create_bet_pool function (ADM-05, ADM-08):
Atomically inserts pool + options in one transaction. Returns the new pool's UUID.
See full implementation in RESEARCH.md Q5 for exact SQL.

    create or replace function public.create_bet_pool(
      p_question  text,
      p_type      text,
      p_deadline  timestamptz,
      p_options   text[]
    )
    returns uuid
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_admin_id uuid    := auth.uid();
      v_pool_id  uuid;
      i          integer;
    begin
      if v_admin_id is null then
        raise exception 'not_authenticated';
      end if;
      if not exists (
        select 1 from public.profiles where id = v_admin_id and is_admin = true
      ) then
        raise exception 'not_admin';
      end if;
      if p_type not in ('binary', 'multiple_exclusive') then
        raise exception 'invalid_pool_type';
      end if;
      if array_length(p_options, 1) < 2 then
        raise exception 'pool_needs_at_least_two_options';
      end if;
      if p_deadline <= now() then
        raise exception 'deadline_must_be_future';
      end if;
      insert into public.bet_pools (question, type, deadline)
      values (p_question, p_type, p_deadline)
      returning id into v_pool_id;
      for i in 1..array_length(p_options, 1) loop
        insert into public.pool_options (pool_id, label, position)
        values (v_pool_id, p_options[i], i);
      end loop;
      insert into public.admin_logs (admin_id, action, details)
      values (
        v_admin_id,
        'pool_created',
        jsonb_build_object(
          'pool_id', v_pool_id,
          'question', p_question,
          'type', p_type,
          'deadline', p_deadline
        )
      );
      return v_pool_id;
    end;
    $$;

    revoke execute on function public.create_bet_pool(text, text, timestamptz, text[]) from public, anon;
    grant execute on function public.create_bet_pool(text, text, timestamptz, text[]) to authenticated;

SECTION 8 — admin_resolve_pool function (ADM-06, ADM-08):
Calls the existing resolve_pool() function internally. Running as SECURITY DEFINER owner
bypasses the REVOKE from authenticated on resolve_pool (RESEARCH.md Q6, Assumption A1).
Fallback: if the inner call raises a permission error in production, replace the
`perform public.resolve_pool(...)` line with the full resolve logic inlined (copy from
migration 0004 SECTION 7). Add a comment in the migration documenting which approach was used.

    create or replace function public.admin_resolve_pool(
      p_pool_id          uuid,
      p_winning_option_id uuid
    )
    returns void
    language plpgsql
    security definer
    set search_path = public
    as $$
    declare
      v_admin_id uuid := auth.uid();
    begin
      if v_admin_id is null then
        raise exception 'not_authenticated';
      end if;
      if not exists (
        select 1 from public.profiles where id = v_admin_id and is_admin = true
      ) then
        raise exception 'not_admin';
      end if;
      if not exists (
        select 1 from public.bet_pools where id = p_pool_id and status = 'open'
      ) then
        raise exception 'pool_not_open_or_not_found';
      end if;
      if not exists (
        select 1 from public.pool_options
        where id = p_winning_option_id and pool_id = p_pool_id
      ) then
        raise exception 'winning_option_not_found';
      end if;
      -- Delegate to existing resolve_pool function (SECURITY DEFINER owner can call it).
      -- If this raises a permission error on your Supabase instance, replace this line
      -- with the resolve_pool logic inlined from migration 0004 SECTION 7.
      perform public.resolve_pool(p_pool_id, p_winning_option_id);
      insert into public.admin_logs (admin_id, action, details)
      values (
        v_admin_id,
        'pool_resolved',
        jsonb_build_object(
          'pool_id', p_pool_id,
          'winning_option_id', p_winning_option_id
        )
      );
    end;
    $$;

    revoke execute on function public.admin_resolve_pool(uuid, uuid) from public, anon;
    grant execute on function public.admin_resolve_pool(uuid, uuid) to authenticated;

Do not add a separate INSERT/UPDATE policy on bet_pools or pool_options — the create_bet_pool
function is SECURITY DEFINER and bypasses RLS entirely, so no policy is needed for pool creation.
  </action>
  <verify>
    <automated>grep -c "private.is_admin" supabase/migrations/0007_admin_panel.sql && grep -c "admin_logs" supabase/migrations/0007_admin_panel.sql && grep -c "not_admin" supabase/migrations/0007_admin_panel.sql && grep -c "revoke execute" supabase/migrations/0007_admin_panel.sql && grep -c "grant execute" supabase/migrations/0007_admin_panel.sql</automated>
  </verify>
  <done>
    - 0007_admin_panel.sql exists at supabase/migrations/0007_admin_panel.sql
    - private schema + private.is_admin() SECURITY DEFINER helper are defined in SECTION 1
    - admin_logs table is created in SECTION 2, before any function definitions
    - profiles_select_admin and token_transactions_select_admin policies use (select private.is_admin())
    - All five functions exist: admin_block_user, admin_grant_tokens, admin_set_match_result, create_bet_pool, admin_resolve_pool
    - Every function has REVOKE from public/anon and GRANT to authenticated immediately after the function body
    - Every function raises 'not_admin' as first guard after null-uid check
    - Every function inserts into admin_logs before returning
    - grep -c "revoke execute" returns at least 5 (one per admin function)
    - grep -c "grant execute" returns at least 5 (one per admin function)
  </done>
</task>

<task type="auto">
  <name>Task 2: Add AdminLog interface to src/types/index.ts</name>
  <files>src/types/index.ts</files>
  <action>
Append to src/types/index.ts. Do not modify existing types — append only. Follow the exact interface style of existing entries (TokenTransaction, Bet, etc.).

In the Database.public.Tables block, add the admin_logs table entry after the token_transactions table:

  admin_logs: {
    Row: {
      id: string;
      admin_id: string;
      action: string;
      target_user_id: string | null;
      details: Record<string, unknown>;
      created_at: string;
    };
    Insert: {
      id?: string;
      admin_id: string;
      action: string;
      target_user_id?: string | null;
      details?: Record<string, unknown>;
      created_at?: string;
    };
    Update: {
      id?: string;
      admin_id?: string;
      action?: string;
      target_user_id?: string | null;
      details?: Record<string, unknown>;
      created_at?: string;
    };
    Relationships: [];
  };

In the Database.public.Functions block, add entries for all five new admin functions:

  admin_block_user: { Args: { p_target_user_id: string; p_blocked: boolean }; Returns: void; };
  admin_grant_tokens: { Args: { p_target_user_id: string; p_amount: number; p_note?: string }; Returns: void; };
  admin_set_match_result: { Args: { p_match_id: string; p_home_score: number; p_away_score: number }; Returns: void; };
  create_bet_pool: { Args: { p_question: string; p_type: string; p_deadline: string; p_options: string[] }; Returns: string; };
  admin_resolve_pool: { Args: { p_pool_id: string; p_winning_option_id: string }; Returns: void; };

After all existing app-level interfaces (after PoolOptionTotal), add:

  export interface AdminLog {
    id: string;
    admin_id: string;
    action: string;
    target_user_id: string | null;
    details: Record<string, unknown>;
    created_at: string;
  }

  // AdminUser extends Profile — used in AdminUsersPage to display all users.
  // Fetched via supabase.from('profiles').select('*') with admin SELECT policy active.
  export type AdminUser = Database['public']['Tables']['profiles']['Row'];
  </action>
  <verify>
    <automated>grep -c "AdminLog" src/types/index.ts && grep -c "admin_logs" src/types/index.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - src/types/index.ts exports AdminLog interface with all six fields (id, admin_id, action, target_user_id, details, created_at)
    - src/types/index.ts exports AdminUser type alias
    - Database.public.Tables contains admin_logs Row/Insert/Update entries
    - Database.public.Functions contains all five new admin function entries
    - npx tsc --noEmit passes with no TypeScript errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→admin RPC | p_target_user_id, p_amount, p_match_id, etc. arrive untrusted from the admin browser |
| admin function→admin_logs | INSERT inside the function is the only write path — no client INSERT policy exists |
| RLS policy→private.is_admin() | RLS reads profiles table via SECURITY DEFINER helper to avoid infinite recursion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | Admin RPC called by non-admin authenticated user | mitigate | Every admin function checks is_admin as first guard; raises 'not_admin'; REVOKE from anon prevents unauthenticated access |
| T-04-02 | Tampering | Token inflation via admin_grant_tokens with extreme amount | mitigate | Function validates current_balance + p_amount >= 0; no upper bound needed (admin-only; audited in admin_logs) |
| T-04-03 | Tampering | Fake admin_logs INSERT from frontend | mitigate | No INSERT policy on admin_logs; all writes come from SECURITY DEFINER functions which run as function owner |
| T-04-04 | Tampering | Recursive RLS on profiles table via admin check in policy | mitigate | private.is_admin() is SECURITY DEFINER — it reads profiles bypassing RLS, breaking the recursion loop |
| T-04-05 | Tampering | profiles_select_admin policy evaluated per-row (performance + logic risk) | mitigate | Policy uses (select private.is_admin()) — SELECT wrapper enables per-statement Postgres initPlan optimization |
| T-04-06 | Elevation of Privilege | create_bet_pool called by non-admin to insert pools | mitigate | Admin guard at top of function; REVOKE from anon; no INSERT policy on bet_pools either way |
| T-04-07 | Tampering | Double-resolve pool via admin_resolve_pool | mitigate | admin_resolve_pool validates pool status = 'open' before delegating to resolve_pool; existing resolve_pool idempotency guard (CR-01) also applies |
| T-04-08 | Repudiation | Admin actions undocumented | mitigate | Every admin function inserts to admin_logs before returning; log cannot be spoofed (T-04-03 above) |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this plan; package legitimacy audit in RESEARCH.md confirms N/A |
</threat_model>

<verification>
After both tasks complete:
1. supabase/migrations/0007_admin_panel.sql exists and contains the string "private.is_admin"
2. grep -c "create table public.admin_logs" supabase/migrations/0007_admin_panel.sql returns 1
3. grep -c "not_admin" supabase/migrations/0007_admin_panel.sql returns at least 5 (one per function)
4. grep -c "revoke execute" supabase/migrations/0007_admin_panel.sql returns at least 5
5. grep -c "grant execute" supabase/migrations/0007_admin_panel.sql returns at least 5
6. grep -c "profiles_select_admin" supabase/migrations/0007_admin_panel.sql returns 1
7. grep -c "token_transactions_select_admin" supabase/migrations/0007_admin_panel.sql returns 1
8. grep -c "AdminLog" src/types/index.ts returns at least 2 (interface + Database row)
9. npx tsc --noEmit passes with no new errors
</verification>

<success_criteria>
- Migration 0007_admin_panel.sql is complete and syntactically valid SQL
- private.is_admin() helper is in the private schema (not exposed via PostgREST)
- admin_logs table has RLS enabled with admin-only SELECT and NO INSERT policy
- profiles_select_admin and token_transactions_select_admin policies use SELECT-wrapped is_admin() call
- All five admin functions have auth check, admin guard, business logic, audit INSERT, REVOKE, and GRANT
- AdminLog interface and AdminUser type are exported from src/types/index.ts
- npx tsc --noEmit passes with no errors
</success_criteria>

<output>
Create `.planning/phases/04-admin-panel-qa-production/04-01-SUMMARY.md` when done
</output>
