---
phase: 07-match-status-knockout
plan: "03"
type: checkpoint
wave: 2
depends_on:
  - "07-01"
files_modified: []
autonomous: false
requirements:
  - admin-create-match-schema-applied

must_haves:
  truths:
    - "admin_create_match function exists in the live Supabase project"
    - "admin_create_match is revoked from public/anon and granted only to authenticated"
    - "Calling admin_create_match as a non-admin authenticated user raises not_admin"
  artifacts: []
  key_links: []
---

<objective>
Human checkpoint: apply migration 0010_admin_create_match.sql to the live Supabase project and verify the new RPC is live, correctly permissioned, and admin-gated before the AdminMatchesPage form (Wave 3) is built against it.

Purpose: The Wave 3 plan adds a "Crear partido" form that calls supabase.rpc('admin_create_match', {...}). That call fails until the function exists in the live database with the correct grants. This checkpoint gates Wave 3 from starting until the schema is confirmed live — mirrors the Phase 5 Wave 1→2 migration/schema-push pattern.
Output: Confirmation that migration 0010 is applied and verified in the live database.
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/phases/07-match-status-knockout/07-01-SUMMARY.md
@/home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0010_admin_create_match.sql
</context>

<tasks>

<task type="checkpoint:human-action">
  <what-built>Migration file supabase/migrations/0010_admin_create_match.sql was created in Wave 1 with the admin_create_match SECURITY DEFINER function (null-auth check, private.is_admin() guard, insert into public.matches with status='scheduled', insert into public.admin_logs, revoke/grant on the authenticated role).</what-built>
  <how-to-verify>
    Step 1 — Apply the migration via Supabase CLI (preferred):
      supabase db push
    OR via Supabase Dashboard: open SQL Editor, paste the contents of supabase/migrations/0010_admin_create_match.sql, and run it.

    Step 2 — Verify the function exists:
      Run in SQL Editor:
        SELECT proname FROM pg_proc WHERE proname = 'admin_create_match';
      Expected: one row returned.

    Step 3 — Verify grants are correct (authenticated only, not anon/public):
      Run in SQL Editor:
        SELECT grantee, privilege_type FROM information_schema.routine_privileges WHERE routine_name = 'admin_create_match';
      Expected: a row granting EXECUTE to "authenticated"; no row granting EXECUTE to "anon" or "PUBLIC".

    Step 4 — Verify the admin guard rejects non-admins:
      As a logged-in NON-admin user (e.g. via the app's browser console or a REST call with that user's JWT), call:
        supabase.rpc('admin_create_match', { p_home_team: 'Test A', p_away_team: 'Test B', p_group_name: 'R16', p_match_datetime: new Date().toISOString() })
      Expected: the call returns an error with message 'not_admin' (no row inserted into public.matches).

    Step 5 — Verify the admin guard allows admins:
      As a logged-in admin user, run the same call as Step 4 with distinct team names (e.g. 'Verify Home' vs 'Verify Away').
      Expected: the call succeeds and returns a uuid. Confirm in Table Editor that a new row appears in public.matches with status = 'scheduled', and a new row appears in public.admin_logs with action = 'create_match'. Delete this test row afterward (DELETE FROM public.matches WHERE home_team = 'Verify Home';) to avoid polluting the calendar.
  </how-to-verify>
  <resume-signal>Type "listo" when all five verification steps pass. If any step fails, describe what you see and Claude will help troubleshoot before continuing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → Supabase cloud DB | Authenticated supabase CLI or dashboard SQL; no untrusted input |
| test RPC calls during verification | Performed by developer with known admin/non-admin test accounts; test rows are deleted after verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | supabase db push | accept | Developer-authenticated CLI operation; function uses `create or replace` so re-applying is idempotent |
| T-07-06 | Elevation of Privilege | admin_create_match grants | mitigate | Step 3 explicitly confirms EXECUTE is revoked from anon/public and granted only to authenticated; Step 4 confirms the in-function is_admin() guard additionally rejects non-admin authenticated callers |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

</threat_model>

<verification>
Human confirms:
- admin_create_match function exists in the live database
- EXECUTE is granted to authenticated only (not anon/public)
- A non-admin authenticated call is rejected with 'not_admin'
- An admin call succeeds, inserts a matches row (status='scheduled') and an admin_logs row (action='create_match')
- Test rows created during verification are deleted before resuming
</verification>

<success_criteria>
Migration 0010 is confirmed live and correctly gated. Wave 3 (AdminMatchesPage "Crear partido" form) may proceed.
</success_criteria>

<output>
Create .planning/phases/07-match-status-knockout/07-03-SUMMARY.md when done
</output>
</content>
