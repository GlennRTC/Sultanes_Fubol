---
phase: 07-match-status-knockout
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0010_admin_create_match.sql
  - src/types/index.ts
autonomous: true
requirements:
  - admin-create-match-db
  - admin-create-match-types

must_haves:
  truths:
    - "Migration file 0010_admin_create_match.sql exists with the admin_create_match SECURITY DEFINER function"
    - "admin_create_match enforces null-auth check, private.is_admin() guard, and writes to admin_logs, matching the pattern of every other admin RPC in 0007_admin_panel.sql"
    - "admin_create_match is registered in Database.public.Functions in src/types/index.ts"
  artifacts:
    - path: "supabase/migrations/0010_admin_create_match.sql"
      provides: "admin_create_match(p_home_team, p_away_team, p_group_name, p_match_datetime, p_venue) RPC"
      contains: "create or replace function public.admin_create_match"
    - path: "src/types/index.ts"
      provides: "TypeScript signature for admin_create_match RPC"
      contains: "admin_create_match"
  key_links:
    - from: "supabase/migrations/0010_admin_create_match.sql"
      to: "private.is_admin()"
      via: "admin guard inside function body"
      pattern: "private\\.is_admin\\(\\)"
    - from: "supabase/migrations/0010_admin_create_match.sql"
      to: "public.admin_logs"
      via: "insert into public.admin_logs"
      pattern: "insert into public\\.admin_logs"
---

<objective>
Create migration 0010_admin_create_match.sql adding the admin_create_match SECURITY DEFINER RPC, and register its signature in the shared TypeScript types file.

Purpose: This is the DB foundation for knockout-stage admin match creation (CONTEXT.md decision 5). The RPC must exist and be typed before the Wave 3 AdminMatchesPage form can call it. This plan has no UI dependency and can be built in parallel with the live-highlight/accordion frontend work (Wave 1 sibling plan 07-02).
Output: supabase/migrations/0010_admin_create_match.sql + updated src/types/index.ts
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/phases/07-match-status-knockout/CONTEXT.md
@/home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0007_admin_panel.sql
@/home/glenn/Denv/Sultanes_Fubol/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0010_admin_create_match.sql</name>
  <files>supabase/migrations/0010_admin_create_match.sql</files>
  <action>
Create the file using the EXACT SQL from CONTEXT.md's "Technical Notes &gt; Admin match creation" section (decision D-05 in CONTEXT.md) — do not alter the function body, parameter order, or security pattern. The function:

1. Is named public.admin_create_match with params p_home_team text, p_away_team text, p_group_name text, p_match_datetime timestamptz, p_venue text default null. Returns uuid. language plpgsql security definer set search_path = public.
2. Null-checks auth.uid() first and raises exception 'not_authenticated' if null (matches the pattern in admin_block_user / admin_set_match_result in 0007_admin_panel.sql).
3. Guards with: if not private.is_admin() then raise exception 'not_admin'; end if; — calls the private.is_admin() helper function directly, exactly as written in CONTEXT.md (not the inline exists(select 1 from public.profiles ...) form used elsewhere in 0007 — CONTEXT.md's SQL for this RPC uses the helper-function call form; preserve it exactly).
4. Validates p_home_team and p_away_team are non-null and non-empty after trim, raising exception 'invalid_teams' on failure.
5. Inserts into public.matches (home_team, away_team, group_name, match_datetime, venue, status) with status hardcoded to 'scheduled', returning the new id into v_match_id.
6. Inserts an audit row into public.admin_logs (admin_id, action, target_user_id, details) with action = 'create_match', target_user_id = null, and a jsonb_build_object containing match_id, home_team, away_team, group_name, match_datetime.
7. Returns v_match_id.
8. Ends with: revoke execute on function public.admin_create_match(text, text, text, timestamptz, text) from public, anon; and grant execute on function public.admin_create_match(text, text, text, timestamptz, text) to authenticated;.

Add a short comment header above the function identifying this as migration 0010, mirroring the header style of 0009_live_scores.sql (purpose line, no transaction wrapper — Supabase applies migrations as-is).

Do NOT add a group_name enum or constraint — CONTEXT.md scope fence: group_name stays free-text (knockout rounds use string labels like 'R16', 'QF', 'SF', 'F'); no schema changes for a "pending team" state.
  </action>
  <verify>
    <automated>grep -c "create or replace function public.admin_create_match" /home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0010_admin_create_match.sql</automated>
  </verify>
  <done>
    - File exists at supabase/migrations/0010_admin_create_match.sql
    - Contains "create or replace function public.admin_create_match"
    - Contains "private.is_admin()" as the admin guard
    - Contains "insert into public.admin_logs" with action 'create_match'
    - Contains both the revoke and grant statements for authenticated role
  </done>
</task>

<task type="auto">
  <name>Task 2: Register admin_create_match in src/types/index.ts</name>
  <files>src/types/index.ts</files>
  <action>
Read src/types/index.ts first to confirm current line numbers (it was already loaded above — use it to locate the exact insertion point in Database.public.Functions).

Add a new entry to the Database.public.Functions object, placed after the existing admin_set_match_result entry (keeps admin RPCs grouped) and before create_bet_pool:

  admin_create_match: { Args: { p_home_team: string; p_away_team: string; p_group_name: string; p_match_datetime: string; p_venue?: string | null }; Returns: string; };

Use `string` for p_match_datetime (the Supabase JS client serializes Date/ISO strings as text over the wire — consistent with how p_deadline is typed as string in create_bet_pool's existing entry). Returns: string because the RPC returns a uuid (Supabase types uuid as string in the JS client, consistent with create_bet_pool's `Returns: string`).

Do NOT create a new types file — src/types/index.ts is the single TypeScript types file per CLAUDE.md. Do NOT add a new Match-related interface; the existing Match interface already covers all fields the new RPC writes (home_team, away_team, group_name, match_datetime, venue, status are all existing Match fields).
  </action>
  <verify>
    <automated>grep -c "admin_create_match" /home/glenn/Denv/Sultanes_Fubol/src/types/index.ts</automated>
  </verify>
  <done>
    - grep returns at least 1 (the Functions entry)
    - The entry's Args match signature: p_home_team, p_away_team, p_group_name, p_match_datetime (all string), p_venue optional string|null
    - tsc --noEmit introduces no new errors related to this change
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → admin_create_match RPC | Only admins may create matches; non-admin callers must be rejected server-side, not just hidden in the UI |
| migration file → Supabase | SQL applied by developer with service-role access; no untrusted input at migration time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | admin_create_match RPC | mitigate | private.is_admin() guard checked before any insert, matching every other admin RPC in 0007_admin_panel.sql — no exceptions |
| T-07-02 | Tampering | admin_logs audit trail | mitigate | INSERT into admin_logs happens inside the same SECURITY DEFINER function as the match insert — no separate unaudited path to create matches |
| T-07-03 | Repudiation | admin_create_match calls | accept | admin_logs records admin_id + full details jsonb; sufficient for after-the-fact accountability, no real-time alerting needed for this internal tool |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

</threat_model>

<verification>
After both tasks:
- supabase/migrations/0010_admin_create_match.sql exists and matches the CONTEXT.md SQL exactly (function signature, security guards, admin_logs insert, revoke/grant)
- src/types/index.ts has the admin_create_match entry in Database.public.Functions
- npx tsc --noEmit exits 0 (no new TypeScript errors)
</verification>

<success_criteria>
Migration file is ready to apply in Wave 2. TypeScript types reflect the new RPC signature so Wave 3's AdminMatchesPage form gets type-checked supabase.rpc('admin_create_match', {...}) calls. No frontend files are modified in this plan.
</success_criteria>

<output>
Create .planning/phases/07-match-status-knockout/07-01-SUMMARY.md when done
</output>
</content>
