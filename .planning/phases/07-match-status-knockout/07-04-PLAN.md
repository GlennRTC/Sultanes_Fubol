---
phase: 07-match-status-knockout
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - src/pages/AdminMatchesPage.tsx
autonomous: true
requirements:
  - admin-create-match-ui

must_haves:
  truths:
    - "An admin can create a new match (e.g. a Round of 16 fixture) via a form in the admin panel — no SQL required"
    - "The form submits home team, away team, round/group label, datetime, and optional venue to admin_create_match"
    - "On success, the new match appears in the page's local state without a full reload"
    - "Validation errors (empty teams, missing datetime) are shown in Spanish before the RPC call fires"
  artifacts:
    - path: "src/pages/AdminMatchesPage.tsx"
      provides: "Crear partido form calling supabase.rpc('admin_create_match', ...)"
      contains: "admin_create_match"
  key_links:
    - from: "src/pages/AdminMatchesPage.tsx"
      to: "admin_create_match RPC"
      via: "supabase.rpc('admin_create_match', {...})"
      pattern: "rpc\\('admin_create_match'"
---

<objective>
Add a "Crear partido" form to AdminMatchesPage.tsx that calls the new admin_create_match RPC, letting admins enter knockout-stage (and any future) fixtures through the UI instead of manual SQL.

Purpose: Per CONTEXT.md decisions 3 and 5 — admins enter the real matchup only once both teams are confirmed, exactly like group-stage matches, and this form replaces manual SQL inserts for knockout fixtures going forward. Depends on Wave 2's confirmed-live schema (07-03) for the RPC to exist and be callable.
Output: Updated src/pages/AdminMatchesPage.tsx with a working match-creation form above the existing results list.
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/phases/07-match-status-knockout/CONTEXT.md
@/home/glenn/Denv/Sultanes_Fubol/src/pages/AdminMatchesPage.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/pages/AdminPoolsPage.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/types/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add "Crear partido" form to AdminMatchesPage.tsx</name>
  <files>src/pages/AdminMatchesPage.tsx</files>
  <action>
Read the current file in full (already loaded above). Add a new form section above the existing "Resultados de Partidos" results list (the `matches.map(match => ...)` block), inside the same `<div className="min-h-[calc(100vh-56px)] bg-zinc-950 px-4 py-6 max-w-4xl mx-auto">` wrapper, following the same visual pattern AdminPoolsPage.tsx uses for its "Crear Pool" section (a `bg-zinc-900 border border-zinc-700 rounded-xl p-4 mb-6` card with an `<h2>` heading and a `<form>` inside using `flex flex-col gap-3`).

Add the following local state alongside the existing `scores`/`submitting`/`error`/`success` state:
- `homeTeam` (string), `awayTeam` (string), `groupLabel` (string), `matchDatetime` (string — datetime-local value, local time, following the exact same `deadline` pattern AdminPoolsPage.tsx uses), `venue` (string), `creating` (boolean).

Form fields, in order:
1. Home team — text input, placeholder "Equipo local".
2. Away team — text input, placeholder "Equipo visitante".
3. Round/group label — text input, placeholder "Grupo o ronda", with a helper paragraph below it reading exactly: "A–L para grupos, o R16/QF/SF/F para eliminatorias" (per CONTEXT.md's exact hint text), styled `text-xs text-zinc-500 mt-1` matching the existing hint-paragraph pattern in AdminPoolsPage.tsx.
4. Datetime — `<input type="datetime-local">`, with a helper paragraph "Hora local. Se guardará en UTC." (same wording AdminPoolsPage.tsx already uses for its deadline field) styled identically.
5. Venue (optional) — text input, placeholder "Sede (opcional)".

On submit (`handleCreateMatch`, calling `e.preventDefault()` first):
- Validate `homeTeam.trim()` and `awayTeam.trim()` are non-empty; if either is empty, `setError('Ingresa ambos equipos.')` and return.
- Validate `groupLabel.trim()` is non-empty; if empty, `setError('Ingresa el grupo o ronda.')` and return.
- Validate `matchDatetime` is non-empty; if empty, `setError('Ingresa la fecha y hora del partido.')` and return.
- Convert `matchDatetime` (datetime-local, local time) to a UTC ISO string exactly as AdminPoolsPage.tsx does for its deadline field: `const matchDatetimeUtc = new Date(matchDatetime).toISOString();`.
- Set `creating(true)`, clear `error`/`success`.
- Call `await supabase.rpc('admin_create_match', { p_home_team: homeTeam.trim(), p_away_team: awayTeam.trim(), p_group_name: groupLabel.trim(), p_match_datetime: matchDatetimeUtc, p_venue: venue.trim() || null })`.
- On error, map RPC error messages to Spanish following the existing pattern in this file's `handleSetScore` and AdminPoolsPage.tsx's `handleCreate`: `'not_admin'` → "No tienes permisos de administrador.", `'invalid_teams'` → "Ingresa ambos equipos.", `'not_authenticated'` → "Tu sesión expiró. Inicia sesión de nuevo.", default → "Error al crear partido. Intenta de nuevo."
- On success: `setSuccess('Partido creado correctamente.')`, reset all five form fields to empty, and prepend the new match to local state so it's immediately visible without a refetch — construct a local `Match` object from the form inputs plus `status: 'scheduled'`, `home_score: null`, `away_score: null`, and the returned id (the RPC's `data` return value is the new match's uuid), then `setMatches(prev => [newMatch, ...prev])`. (This new match will only show in the results list if it matches the existing fetch's status filter `in('status', ['scheduled', 'live'])` — since it's always created as 'scheduled', it always qualifies.)
- Set `creating(false)` in all paths (success and error).

Style the submit button identically to AdminPoolsPage.tsx's "Crear pool" button: `min-h-[44px] w-full bg-emerald-500 hover:bg-emerald-600 disabled:opacity-50 text-white font-bold rounded-lg flex items-center justify-center gap-2`, showing a `Loader2` spinner (already imported) while `creating` is true, otherwise the label "Crear partido".

Do not modify the existing results-list rendering, `handleSetScore`, or the score-input fields below — this task only adds the new form section above them.
  </action>
  <verify>
    <automated>grep -c "admin_create_match" /home/glenn/Denv/Sultanes_Fubol/src/pages/AdminMatchesPage.tsx</automated>
  </verify>
  <done>
    - AdminMatchesPage.tsx contains a "Crear partido" form with inputs for home team, away team, group/round label (with the exact hint text), datetime-local, and optional venue
    - Form calls supabase.rpc('admin_create_match', {...}) with p_match_datetime as a UTC ISO string converted from the datetime-local input
    - Validation errors for empty teams, empty group/round label, and missing datetime are shown in Spanish before the RPC fires
    - On success, the new match is prepended to local matches state and a success message is shown
    - npx tsc --noEmit passes with no new errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser → admin_create_match RPC | Form inputs are untrusted client input; all real validation (admin check, non-empty teams) happens server-side in the RPC, this form's client-side checks are UX-only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | Crear partido form inputs | accept | Client-side validation is UX-only; the admin_create_match RPC re-validates non-empty teams and the is_admin() guard server-side regardless of what the client sends |
| T-07-08 | Information Disclosure | RPC error messages surfaced to UI | accept | Error messages are generic Spanish strings mapped from known error codes ('not_admin', 'invalid_teams', 'not_authenticated'); no raw DB error text or stack traces are shown to the user |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |

</threat_model>

<verification>
- npx tsc --noEmit exits 0 (no new TypeScript errors)
- Manual smoke test: log in as admin, open /admin/partidos (or equivalent route), fill the Crear partido form with a knockout fixture (e.g. "México" vs "Brasil", group "R16", a future datetime), submit, confirm success message and the new match appears in the page without reload
- Manual smoke test: submit with an empty team field, confirm a Spanish validation error appears and no RPC call is made
</verification>

<success_criteria>
An admin can create a new match (including knockout-stage fixtures with round labels like R16/QF/SF/F) via the AdminMatchesPage UI, with no SQL required. The new match immediately appears on /calendario once created, since it inserts directly into public.matches with status='scheduled'.
</success_criteria>

<output>
Create .planning/phases/07-match-status-knockout/07-04-SUMMARY.md when done
</output>
</content>
