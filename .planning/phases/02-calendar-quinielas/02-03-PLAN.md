---
phase: 02-calendar-quinielas
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/TimezonePicker.tsx
  - src/components/MatchCard.tsx
  - src/components/PredictionModal.tsx
  - src/pages/CalendarPage.tsx
autonomous: false
requirements: [CAL-01, CAL-02, CAL-03, CAL-04, QUI-01, QUI-02, QUI-03]
must_haves:
  truths:
    - "User can see all 72 matches on /calendario, toggling between Por fecha and Por grupo views"
    - "Match times display in the user's local timezone; changing it via Cambiar persists in localStorage"
    - "Por fecha view filters by group and by team via two dropdowns with a Todos reset"
    - "Each match card shows a status badge (Programado / En vivo / Finalizado)"
    - "Clicking a scheduled match opens a modal; submitting a score deducts 20 tokens and shows a prediction badge"
    - "Predicting on a live/finished match is blocked with the Spanish locked-match message"
  artifacts:
    - path: "src/pages/CalendarPage.tsx"
      provides: "/calendario page: fetch matches+predictions, timezone, view toggle, filters, modal orchestration"
      min_lines: 80
    - path: "src/components/MatchCard.tsx"
      provides: "match card with local time, status badge, prediction badge"
      min_lines: 25
    - path: "src/components/PredictionModal.tsx"
      provides: "two-step prediction modal calling place_prediction RPC"
      min_lines: 60
    - path: "src/components/TimezonePicker.tsx"
      provides: "timezone selector + detectTimezone/saveTimezone + SUPPORTED_TIMEZONES"
      min_lines: 30
  key_links:
    - from: "PredictionModal"
      to: "place_prediction RPC"
      via: "supabase.rpc"
      pattern: "rpc\\('place_prediction'"
    - from: "PredictionModal success"
      to: "authStore.updateTokens"
      via: "imperative store call before close"
      pattern: "updateTokens\\(-20\\)"
    - from: "CalendarPage / MatchCard"
      to: "user timezone"
      via: "date-fns-tz v3 formatInTimeZone / toZonedTime"
      pattern: "formatInTimeZone|toZonedTime"
---

<objective>
Deliver the core quinielas vertical slice: the `/calendario` page with both views (Por fecha date-grouped, Por grupo tabbed A–L), local-timezone match times, group/team filters, status badges, and a two-step prediction modal that calls `place_prediction` and updates the token balance. After this plan, an authenticated user can browse all 72 matches and place a prediction end to end (the route wiring lands in Plan 04).

Purpose: This is the user-facing heart of Phase 2 — CAL-01–04 + QUI-01–03 become real and exercisable.

Output: `TimezonePicker.tsx`, `MatchCard.tsx`, `PredictionModal.tsx`, `CalendarPage.tsx`.
</objective>

<execution_context>
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-calendar-quinielas/02-CONTEXT.md
@.planning/phases/02-calendar-quinielas/02-RESEARCH.md
@.planning/phases/02-calendar-quinielas/02-PATTERNS.md
@.planning/phases/02-calendar-quinielas/02-UI-SPEC.md
@src/pages/LoginPage.tsx
@src/pages/RegistroPage.tsx
@src/lib/supabase.ts
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: TimezonePicker + timezone utilities</name>
  <files>src/components/TimezonePicker.tsx</files>
  <read_first>
    - .planning/phases/02-calendar-quinielas/02-PATTERNS.md §`src/components/TimezonePicker.tsx` (SUPPORTED_TIMEZONES, detectTimezone, saveTimezone)
    - .planning/phases/02-calendar-quinielas/02-RESEARCH.md §Code Examples (Timezone Detection, lines 723-764) and Pitfall 8
    - .planning/phases/02-calendar-quinielas/02-UI-SPEC.md §Timezone Picker + §Color
    - src/components/Navbar.tsx (named-export component style)
  </read_first>
  <action>
    Create `src/components/TimezonePicker.tsx`. Export the constant `SUPPORTED_TIMEZONES` (the exact 15 entries from PATTERNS §TimezonePicker — labels in Spanish, IANA names: America/Mexico_City, America/Mazatlan, America/Cancun, America/Bogota, America/Lima, America/Guayaquil, America/Caracas, America/La_Paz, America/Santiago, America/Argentina/Buenos_Aires, America/Asuncion, America/Havana, Europe/Madrid, Atlantic/Canary, UTC). Export `STORAGE_KEY = 'fubol_timezone'` (D-15) and helper functions `detectTimezone(): string` (localStorage first → `Intl.DateTimeFormat().resolvedOptions().timeZone` if in the supported set → fallback 'UTC', per Pitfall 8) and `saveTimezone(iana: string): void` (writes localStorage). Export a `TimezonePicker` component with props `{ current: string; onSelect: (iana: string) => void; onClose: () => void }` rendered as a centered modal overlay per UI-SPEC: backdrop `fixed inset-0 bg-black/60 flex items-center justify-center z-50 px-4`; panel `bg-slate-800 border border-slate-700 rounded-xl p-4 w-full max-w-[320px]`; heading `text-sm font-bold text-slate-300 mb-3` = "Selecciona tu zona horaria"; each option a `<button>` `w-full text-left px-3 py-2 text-sm text-slate-100 hover:bg-slate-700 rounded-lg min-h-[44px]` (active option adds `bg-slate-700`). Selecting an option calls `onSelect(iana)` then `onClose()`. Close on Escape key (UI-SPEC accessibility). Use only the declared Tailwind palette; no new colors.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "CalendarPage\|LeaderboardPage\|MatchCard\|PredictionModal" | grep -c "error TS" | grep -q '^0$' && grep -q "SUPPORTED_TIMEZONES" src/components/TimezonePicker.tsx && grep -q "fubol_timezone" src/components/TimezonePicker.tsx && grep -c "iana:" src/components/TimezonePicker.tsx | grep -q "^15$" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `SUPPORTED_TIMEZONES` exports exactly 15 entries with the IANA names listed above.
    - `detectTimezone` reads localStorage → browser zone → 'UTC' fallback; `saveTimezone` writes the `fubol_timezone` key.
    - `TimezonePicker` renders the 15 options, calls onSelect+onClose, closes on Escape, uses only declared palette classes.
  </acceptance_criteria>
  <done>TimezonePicker compiles; verify prints PASS; 15 timezone entries present.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: MatchCard + PredictionModal</name>
  <files>src/components/MatchCard.tsx, src/components/PredictionModal.tsx</files>
  <read_first>
    - .planning/phases/02-calendar-quinielas/02-PATTERNS.md §`src/components/MatchCard.tsx` and §`src/components/PredictionModal.tsx` (full component patterns + RPC error map)
    - .planning/phases/02-calendar-quinielas/02-UI-SPEC.md §Match card, §Prediction Modal, §Confirmation Step, §Interaction States, §Accessibility Contract, §Copywriting Contract
    - .planning/phases/02-calendar-quinielas/02-RESEARCH.md §Pattern 1, §Pattern 2 (error names), §Pitfall 5, §Pitfall 6
    - src/pages/RegistroPage.tsx (two-step async form + loading button + error banner pattern)
    - src/store/authStore.ts (updateTokens action added in Plan 01)
  </read_first>
  <action>
    `MatchCard.tsx`: named export `MatchCard({ match, prediction, timezone, onCardClick }: { match: Match; prediction?: Prediction; timezone: string; onCardClick: (m: Match) => void })`. Container per UI-SPEC: `bg-slate-800 border border-slate-700 rounded-xl p-4 mb-2 cursor-pointer hover:border-slate-500 transition-colors min-h-[72px] flex flex-col gap-1`, whole card clickable (D-05/D-09 — cards always clickable, even locked). Teams row shows home (bold), then `home_score - away_score` if status==='finished' else "vs", then away (bold). Meta row: match time via `formatInTimeZone(new Date(match.match_datetime), timezone, 'd MMM · HH:mm zzz')` from `date-fns-tz` (v3 — NEVER utcToZonedTime), plus a status badge pill: Programado `text-slate-400 bg-slate-700`, En vivo `text-green-400 bg-green-900/50`, Finalizado `text-slate-500 bg-slate-800`. If `prediction` present, render the badge `inline-flex items-center bg-slate-700 rounded-full px-2 py-0.5 text-xs text-green-400` = "Tu predicción: {home}-{away}".

    `PredictionModal.tsx`: named export `PredictionModal({ match, existingPrediction, timezone, onClose, onSuccess }: { match: Match; existingPrediction?: Prediction; timezone: string; onClose: () => void; onSuccess: (p: { home_score_prediction: number; away_score_prediction: number }) => void })`. Use `step` state `'input' | 'confirm'` plus `homeScore`, `awayScore` strings, `error`, `loading`. Overlay + panel + close "×" button per UI-SPEC (role="dialog", aria-modal="true", aria-labelledby on the match header). Header = "{home_team} vs {away_team}" + date/time line via formatInTimeZone with `"d 'de' MMMM · HH:mm zzz"` (import `es` from `date-fns/locale`). Determine `isLocked = match.status !== 'scheduled'`. THREE render modes:
      (a) existingPrediction present → read-only: score inputs disabled+prefilled, no submit button, a "Cerrar" link.
      (b) isLocked and no existingPrediction → show "Este partido ya comenzó — no se aceptan predicciones." (D-09) and a "Cerrar" link, inputs disabled.
      (c) scheduled and no existingPrediction → editable: two number inputs `type="number" min="0" max="99"` (UI-SPEC classes), "Costo: 20 fichas" reminder, primary "Confirmar predicción" button → sets step='confirm'. In confirm step, swap to the confirmation copy "¿Estás seguro? Esta predicción no se puede cambiar. Se descontarán 20 fichas." with final "Confirmar" button + "Cancelar" link.
    On final Confirmar: `setLoading(true)`, call `supabase.rpc('place_prediction', { p_match_id: match.id, p_home_score: parseInt(homeScore), p_away_score: parseInt(awayScore) })`. On error map per PATTERNS: message includes `insufficient_tokens` → "No tienes suficientes fichas para hacer esta predicción."; includes `match_not_scheduled` → "Este partido ya comenzó — no se aceptan predicciones."; `code === '23505'` → "Ya tienes una predicción para este partido."; else "No se pudo guardar tu predicción. Intenta de nuevo." (set step back to 'input', loading false). On success: call `useAuthStore.getState().updateTokens(-20)` BEFORE closing (Pitfall 5), then `onSuccess({ home_score_prediction, away_score_prediction })` and `onClose()`. Reuse the error banner pattern `role="alert" bg-red-900/50 border border-red-700` and the inline spinner on the button (`aria-busy`). All copy in Spanish per Copywriting Contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -v "CalendarPage\|LeaderboardPage" | grep -c "error TS" | grep -q '^0$' && grep -q "formatInTimeZone" src/components/MatchCard.tsx && grep -q "rpc('place_prediction'" src/components/PredictionModal.tsx && grep -q "updateTokens(-20)" src/components/PredictionModal.tsx && grep -q "no se aceptan predicciones" src/components/PredictionModal.tsx && ! grep -q "utcToZonedTime" src/components/MatchCard.tsx src/components/PredictionModal.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `MatchCard` renders teams, local time via `formatInTimeZone`, the correct status badge, and a prediction badge when a prediction exists; card is always clickable.
    - `PredictionModal` has the three render modes (read-only existing / locked-no-prediction / editable two-step) and the exact Spanish copy.
    - Submit calls `supabase.rpc('place_prediction', ...)`, maps `insufficient_tokens`/`match_not_scheduled`/`23505`/generic errors to the specified Spanish messages, and calls `updateTokens(-20)` before closing.
    - No use of `utcToZonedTime` or `zonedTimeToUtc` (v2 API) anywhere.
    - Modal has role="dialog", aria-modal, aria-labelledby; inputs use min/max 0–99.
  </acceptance_criteria>
  <done>Both components compile; verify prints PASS; error map and updateTokens wiring present; no v2 date-fns-tz API.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: CalendarPage — fetch, views, filters, modal orchestration</name>
  <files>src/pages/CalendarPage.tsx</files>
  <read_first>
    - .planning/phases/02-calendar-quinielas/02-PATTERNS.md §`src/pages/CalendarPage.tsx` (fetch-on-mount, Promise.all, error/loading) and §Code Examples (groupMatchesByLocalDate, formatDateHeader)
    - .planning/phases/02-calendar-quinielas/02-UI-SPEC.md §CalendarPage (layout, timezone bar, view toggle, filter bar, group tabs, date headers, empty states)
    - .planning/phases/02-calendar-quinielas/02-RESEARCH.md §Pitfall 2 (group by LOCAL date), §WC count (72 / groups A–L), §Anti-Patterns (fetch both at page level)
    - src/pages/LoginPage.tsx (fetch/loading/error pattern)
    - src/components/MatchCard.tsx, src/components/PredictionModal.tsx, src/components/TimezonePicker.tsx (Tasks 1-2 of this plan)
  </read_first>
  <action>
    Create `src/pages/CalendarPage.tsx` (named export `CalendarPage`). Page wrapper `min-h-[calc(100vh-56px)] bg-slate-900 px-4 py-6`; heading `text-xl font-bold text-slate-100 mb-4` = "Calendario". On mount, fetch both in parallel: `supabase.from('matches').select('*').order('match_datetime')` and `supabase.from('predictions').select('*')` (Promise.all; predictions filtered to current user by `predictions_select_own` RLS). Maintain loading + error state; on fetch error show "No se pudieron cargar los partidos. Recarga la página." via the role="alert" banner; while loading reuse the inline spinner pattern.

    Timezone: initialize `timezone` state from `detectTimezone()`; render the timezone bar `text-sm text-slate-400` = "Zona: {display name} · " + a "Cambiar" `<button class="text-green-400 hover:text-green-300 underline text-sm">`. Clicking Cambiar opens `TimezonePicker`; on select, call `saveTimezone(iana)` and update state (D-14/D-15). Resolve display name from SUPPORTED_TIMEZONES.

    View toggle (D-01): `view` state `'fecha' | 'grupo'`, two toggle buttons per UI-SPEC (active `bg-slate-700 text-slate-100 font-bold`, inactive transparent).
      - Por fecha: render two native `<select>` dropdowns "Grupo:" and "Equipo:" each with a "Todos" option (D-03); group options A–L; team options = sorted unique team names from the 72 matches. Apply client-side filtering on the fetched array. Group the FILTERED matches by LOCAL date using `toZonedTime(new Date(match_datetime), timezone)` then `format(zonedDate,'yyyy-MM-dd')` as the Map key (Pitfall 2 — never substring the UTC string). Render a date header per group `text-sm font-bold text-slate-300 mt-6 mb-2 uppercase tracking-wide` formatted "d 'de' MMMM" with `es` locale, followed by that day's MatchCards.
      - Por grupo: render a horizontally-scrollable tab row for groups A through L (12 tabs — NOT A–H), `flex gap-1 overflow-x-auto`, active/inactive styles per UI-SPEC, labels "Grupo A".."Grupo L"; show the selected group's 6 matches as MatchCards.
    Empty state when filters match nothing: heading "Sin partidos" + body "Ningún partido coincide con los filtros seleccionados. Selecciona «Todos» para ver todos."

    Modal orchestration: track `selectedMatch` state; clicking any MatchCard sets it (cards always clickable, even locked). Render `PredictionModal` when set, passing the match, the user's existing prediction for that match (lookup in predictions array by match_id), the timezone, `onClose` (clear selectedMatch), and `onSuccess` (append the new prediction to local predictions state so the card badge appears immediately — D-08). Pass `prediction` to each MatchCard so badges render.

    Use only declared palette/typography/spacing from UI-SPEC. All copy in Spanish.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q '^0$' && grep -q "from('matches')" src/pages/CalendarPage.tsx && grep -q "from('predictions')" src/pages/CalendarPage.tsx && grep -q "toZonedTime" src/pages/CalendarPage.tsx && grep -q "Grupo L\|'L'" src/pages/CalendarPage.tsx && grep -q "Por fecha" src/pages/CalendarPage.tsx && grep -q "Por grupo" src/pages/CalendarPage.tsx && ! grep -q "substring" src/pages/CalendarPage.tsx && npm run build >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - CalendarPage fetches matches and predictions in parallel and handles loading/error.
    - Both views render: Por fecha (date-grouped by LOCAL date, with group/team Todos filters) and Por grupo (tabs A–L, 12 groups).
    - Timezone bar shows current zone + Cambiar opening TimezonePicker; selection persists via saveTimezone.
    - Clicking a card opens PredictionModal with the user's existing prediction (if any); successful submission adds the prediction to local state so the badge appears.
    - Grouping uses `toZonedTime` local-date keys, never UTC substring.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>CalendarPage compiles and `npm run build` passes; verify prints PASS; both views and 12 group tabs present; local-date grouping used.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: [CHECKPOINT] Verify calendar + prediction flow in the browser</name>
  <files>src/pages/CalendarPage.tsx, src/components/PredictionModal.tsx</files>
  <action>
    Claude has already built and compiled the calendar slice in Tasks 1-3; this task is the human verification gate — no further code is written unless a check below surfaces a defect. Present the verification steps to the user and pause for approval. If the `/calendario` route is not yet mounted (route wiring is Plan 04), instruct the user to verify after Plan 04 ships, or temporarily render `CalendarPage` to confirm the flow.
  </action>
  <what-built>The /calendario page with both views, timezone switching, filters, status badges, and the two-step prediction modal wired to place_prediction. (Route wiring lands in Plan 04, so temporarily navigate via the component or wait until Plan 04 if the route is not yet mounted.)</what-built>
  <how-to-verify>
    1. After Plan 04 mounts the route (or via a temporary direct render), open `/calendario` while logged in.
    2. Confirm all 72 matches appear; switch Por fecha ↔ Por grupo; confirm 12 group tabs (A–L) and that Argentina, Francia, Portugal, Inglaterra appear (groups I–L).
    3. Confirm match times show in your local zone; click "Cambiar", pick "Argentina / Uruguay", confirm times shift and persist after refresh.
    4. In Por fecha, use the Grupo and Equipo dropdowns; confirm filtering and the "Todos" reset.
    5. Click a scheduled match → enter a score → "Confirmar predicción" → confirm the "¿Estás seguro?" step → "Confirmar". Confirm the modal closes, the card shows "Tu predicción: N-N", and the Navbar "Fichas" balance drops by 20 immediately.
    6. Click a match you already predicted → confirm read-only modal with your score.
  </how-to-verify>
  <verify>
    <human-check>72 matches visible; 12 group tabs; timezone switch persists; filters work; prediction deducts 20 tokens and shows badge; locked/already-predicted matches show read-only modal.</human-check>
  </verify>
  <acceptance_criteria>
    - User confirms 72 matches visible across both views with 12 group tabs (A-L).
    - Timezone switch persists across refresh.
    - Group/team filters work with the Todos reset.
    - A prediction deducts 20 tokens immediately and shows the badge; locked/already-predicted matches render read-only.
  </acceptance_criteria>
  <done>User types "approved" after confirming the calendar and prediction flow behave as specified.</done>
  <resume-signal>Type "approved" if the calendar and prediction flow work, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → place_prediction RPC | Score values + match id cross from untrusted browser input |
| browser → localStorage | Timezone preference stored client-side (no trust impact — display only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-V | Input Validation | PredictionModal score inputs | mitigate | `type="number" min="0" max="99"` on inputs + DB `check (score >= 0)`; backend re-validates |
| T-02-03-T | Tampering | prediction submission | mitigate | Submission only via `supabase.rpc('place_prediction')`; never a direct `predictions` insert (CLAUDE.md) |
| T-02-03-B | Business logic | locked-match prediction | mitigate | Frontend disables inputs when `status !== 'scheduled'`; place_prediction raises `match_not_scheduled` as the authoritative gate (QUI-02) |
| T-02-03-SC | Tampering | package installs | accept | No new packages — all imports from already-installed date-fns-tz/date-fns/supabase-js/zustand |
</threat_model>

<verification>
- `/calendario` renders 72 matches in both views with 12 group tabs (A–L).
- Local timezone display + persisted switching via Cambiar.
- Group/team filters with Todos reset.
- Two-step prediction modal calls place_prediction, deducts 20 tokens, shows badge; locked matches read-only.
- `npm run build` passes.
</verification>

<success_criteria>
- CAL-01–04 and QUI-01–03 are exercisable: browse all matches, see local times + status, filter, and place a prediction with immediate token deduction.
</success_criteria>

<output>
Create `.planning/phases/02-calendar-quinielas/02-03-SUMMARY.md` when done.
</output>
