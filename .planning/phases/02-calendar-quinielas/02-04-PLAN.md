---
phase: 02-calendar-quinielas
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/pages/LeaderboardPage.tsx
  - src/App.tsx
  - src/components/Navbar.tsx
  - src/pages/LoginPage.tsx
  - src/pages/RegistroPage.tsx
autonomous: false
requirements: [QUI-04, QUI-05, CAL-01]
must_haves:
  truths:
    - "/tabla shows a ranked leaderboard of all users by leaderboard_points from leaderboard_view"
    - "/calendario and /tabla are reachable protected routes; / and /bienvenido redirect to /calendario"
    - "Navbar shows Calendario and Tabla nav links with active state"
    - "After auth, /login and /registro redirect to /calendario (not /bienvenido)"
    - "After calculate_prediction_points runs, the leaderboard reflects updated points"
  artifacts:
    - path: "src/pages/LeaderboardPage.tsx"
      provides: "/tabla ranked table from leaderboard_view"
      min_lines: 40
    - path: "src/App.tsx"
      provides: "calendario + tabla routes; redirects updated"
      contains: "/calendario"
    - path: "src/components/Navbar.tsx"
      provides: "Calendario + Tabla NavLinks"
      contains: "Tabla"
  key_links:
    - from: "LeaderboardPage"
      to: "leaderboard_view"
      via: "supabase.from('leaderboard_view')"
      pattern: "from\\('leaderboard_view'\\)"
    - from: "App routes"
      to: "CalendarPage + LeaderboardPage"
      via: "protected layout routes"
      pattern: "path=\"/tabla\""
---

<objective>
Wire Phase 2 into the app shell and deliver the leaderboard slice: create `/tabla` (ranked table from `leaderboard_view`), mount `/calendario` and `/tabla` as protected routes, replace the `/bienvenido` HomePage placeholder (redirect `/` and `/bienvenido` → `/calendario`), add Navbar nav links, and update the post-auth redirect targets in App/Login/Registro. After this plan the full Phase 2 experience is reachable end to end.

Purpose: Completes QUI-05 (leaderboard), QUI-04's user-visible result (points appear after admin scoring), and the D-17/D-18/D-19 routing decisions. This plan owns `App.tsx` (which imports CalendarPage from Plan 03), so it runs after Plan 03.

Output: `LeaderboardPage.tsx`, updated `App.tsx`, `Navbar.tsx`, `LoginPage.tsx`, `RegistroPage.tsx`.
</objective>

<execution_context>
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-calendar-quinielas/02-CONTEXT.md
@.planning/phases/02-calendar-quinielas/02-RESEARCH.md
@.planning/phases/02-calendar-quinielas/02-PATTERNS.md
@.planning/phases/02-calendar-quinielas/02-UI-SPEC.md
@src/App.tsx
@src/components/Navbar.tsx
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: LeaderboardPage (/tabla)</name>
  <files>src/pages/LeaderboardPage.tsx</files>
  <read_first>
    - .planning/phases/02-calendar-quinielas/02-PATTERNS.md §`src/pages/LeaderboardPage.tsx` (query + table render)
    - .planning/phases/02-calendar-quinielas/02-UI-SPEC.md §LeaderboardPage (table layout, columns, own-user highlight, empty state)
    - .planning/phases/02-calendar-quinielas/02-RESEARCH.md §Pattern 4, §Pitfall 7 (use leaderboard_view, not profiles)
    - src/pages/LoginPage.tsx (fetch/loading/error pattern)
    - src/store/authStore.ts (current user id for own-row highlight)
  </read_first>
  <action>
    Create `src/pages/LeaderboardPage.tsx` (named export `LeaderboardPage`). Page wrapper `min-h-[calc(100vh-56px)] bg-slate-900 px-4 py-6`; heading `text-xl font-bold text-slate-100 mb-4` = "Tabla de posiciones". On mount, query `supabase.from('leaderboard_view').select('id, username, tokens, leaderboard_points').order('leaderboard_points', { ascending: false }).limit(100)` (MUST use `leaderboard_view`, NOT `profiles` — Pitfall 7). Maintain loading + error state; on error show "No se pudo cargar la tabla. Recarga la página." via the role="alert" banner; while loading reuse the inline spinner. Render the table per UI-SPEC: outer `bg-slate-800 border border-slate-700 rounded-xl overflow-hidden`; header row columns "#" (w-8), "Usuario" (flex-1), "Puntos" (w-20 text-right), "Fichas" (w-20 text-right); data rows with rank index (1-based), username, `leaderboard_points` (bold), `tokens` (muted). Highlight the current user's row (`useAuthStore` user.id === entry.id) with `bg-slate-700/50`. Empty state (no rows / all zero): heading "Aún no hay puntos registrados" + body "Los puntos se actualizan cuando el administrador ingresa los resultados de los partidos." Use only declared palette/typography. All copy in Spanish.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error TS" | grep -q '^0$' && grep -q "from('leaderboard_view')" src/pages/LeaderboardPage.tsx && grep -q "Tabla de posiciones" src/pages/LeaderboardPage.tsx && grep -q "leaderboard_points" src/pages/LeaderboardPage.tsx && ! grep -q "from('profiles')" src/pages/LeaderboardPage.tsx && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Queries `leaderboard_view` (never `profiles`), ordered by `leaderboard_points` desc, limit 100.
    - Renders ranked table with #, Usuario, Puntos, Fichas columns and own-user row highlight.
    - Empty-state copy present; error/loading handled.
  </acceptance_criteria>
  <done>LeaderboardPage compiles; verify prints PASS; uses leaderboard_view.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: App routing + Navbar links + auth redirect targets</name>
  <files>src/App.tsx, src/components/Navbar.tsx, src/pages/LoginPage.tsx, src/pages/RegistroPage.tsx</files>
  <read_first>
    - src/App.tsx (current route tree, layout route, HomePage import)
    - src/components/Navbar.tsx (current structure — insert links between brand and user-info)
    - .planning/phases/02-calendar-quinielas/02-PATTERNS.md §`src/App.tsx` and §`src/components/Navbar.tsx` (exact edits, D-17/18/19)
    - .planning/phases/02-calendar-quinielas/02-UI-SPEC.md §Navbar Updates (link classes + active state)
    - src/pages/LoginPage.tsx lines 25-39, src/pages/RegistroPage.tsx lines 43-46 (bienvenido redirect targets)
  </read_first>
  <action>
    `src/App.tsx` (D-18): import `CalendarPage` from `./pages/CalendarPage` and `LeaderboardPage` from `./pages/LeaderboardPage`; remove the `HomePage` import and its route. Inside the protected layout route add `<Route path="/calendario" element={<CalendarPage />} />` and `<Route path="/tabla" element={<LeaderboardPage />} />`. Replace `<Route path="/" element={<Navigate to="/bienvenido" replace />} />` with `to="/calendario"`, and add `<Route path="/bienvenido" element={<Navigate to="/calendario" replace />} />` (so the old bookmark still works). Leave HomePage.tsx on disk but unimported (or delete it).

    `src/components/Navbar.tsx` (D-17): import `NavLink` from `react-router-dom`; insert a links container between the brand `<span>` and the user-info `<div>`: container `flex items-center gap-2`; each NavLink uses UI-SPEC classes — inactive `text-sm text-slate-300 hover:text-white px-3 py-1 rounded-lg min-h-[44px] flex items-center`, active `text-sm text-green-400 px-3 py-1 rounded-lg min-h-[44px] flex items-center` (via the `({ isActive }) => ...` className form). Links: "Calendario" → `/calendario`, "Tabla" → `/tabla`.

    `src/pages/LoginPage.tsx` (D-19): change the default `from` fallback (line 26) and the authenticated-redirect target (line 39) from `'/bienvenido'` to `'/calendario'`. `src/pages/RegistroPage.tsx` (D-19): change the authenticated-redirect target (line 46) from `'/bienvenido'` to `'/calendario'`. Do not change any other logic in those two files.
  </action>
  <verify>
    <automated>grep -q 'path="/calendario"' src/App.tsx && grep -q 'path="/tabla"' src/App.tsx && grep -q 'to="/calendario"' src/App.tsx && ! grep -q "HomePage" src/App.tsx && grep -q "NavLink" src/components/Navbar.tsx && grep -q "Calendario" src/components/Navbar.tsx && grep -q "Tabla" src/components/Navbar.tsx && ! grep -q "bienvenido', { replace" src/pages/LoginPage.tsx && ! grep -q "bienvenido', { replace" src/pages/RegistroPage.tsx && npm run build >/dev/null 2>&1 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `/calendario` and `/tabla` are protected routes; `/` and `/bienvenido` redirect to `/calendario`; HomePage no longer imported in App.tsx.
    - Navbar renders Calendario + Tabla NavLinks with active-state styling.
    - LoginPage `from` fallback and redirect, and RegistroPage redirect, all target `/calendario`.
    - `npm run build` succeeds.
  </acceptance_criteria>
  <done>Routing, Navbar, and redirects updated; verify prints PASS; build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Verify routing + leaderboard + scoring</name>
  <files>src/App.tsx, src/pages/LeaderboardPage.tsx</files>
  <action>
    Claude has already wired routing and built the leaderboard in Tasks 1-2; this task is the human verification gate plus a manual scoring trigger. Present the verification steps to the user and pause for approval. The `calculate_prediction_points` call in step 4 is run by the user from the Supabase dashboard (service-role only — not callable from the app); Claude does not run it. No further code is written unless a check surfaces a defect.
  </action>
  <what-built>Full Phase 2 wiring: /calendario as the post-login home, /tabla leaderboard from leaderboard_view, Navbar links, and updated redirects. Plus a manual scoring check using calculate_prediction_points (QUI-04 → QUI-05).</what-built>
  <how-to-verify>
    1. Log in → confirm you land on `/calendario` (not `/bienvenido`). Visit `/bienvenido` and `/` directly → both redirect to `/calendario`.
    2. Confirm the Navbar shows "Calendario" and "Tabla" links; the active link is highlighted green.
    3. Place at least one prediction (Plan 03 flow), then open `/tabla` → confirm it loads (likely showing the empty-state copy if no results scored yet).
    4. In the Supabase dashboard SQL editor, run `select calculate_prediction_points('<a match_id you predicted>', <home>, <away>);` choosing scores that make your prediction an exact match.
    5. Refresh `/tabla` → confirm your username now shows points (3 for exact) and your row is highlighted; the Navbar "Fichas" increases by 30 after the next profile refresh.
    6. Confirm the leaderboard shows other users too (cross-user read works via leaderboard_view) and does NOT leak admin/blocked fields.
  </how-to-verify>
  <verify>
    <human-check>Post-login lands on /calendario; redirects work; Navbar links + active state correct; /tabla loads; after calculate_prediction_points the leaderboard reflects updated points and the user row highlights; no admin/blocked data exposed.</human-check>
  </verify>
  <acceptance_criteria>
    - Post-login lands on /calendario; /bienvenido and / redirect to /calendario.
    - Navbar shows Calendario + Tabla links with active-state highlight.
    - /tabla loads from leaderboard_view; after calculate_prediction_points the user row shows updated points and is highlighted.
    - No is_admin/is_blocked/email exposed in the leaderboard.
  </acceptance_criteria>
  <done>User types "approved" after confirming routing, the leaderboard, and post-scoring point updates behave as specified.</done>
  <resume-signal>Type "approved" if routing, leaderboard, and scoring all work, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → leaderboard_view read | Authenticated user reads aggregated cross-user data |
| service-role → calculate_prediction_points | Admin-only scoring trigger (dashboard in Phase 2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-ID | Information Disclosure | LeaderboardPage query | mitigate | Reads `leaderboard_view` (username/tokens/leaderboard_points only) — never `profiles`; no is_admin/is_blocked/email exposure |
| T-02-04-EOP | Elevation of Privilege | scoring trigger | mitigate | `calculate_prediction_points` has no authenticated grant; only invoked from the Supabase dashboard / service role in this phase |
| T-02-04-AC | Access Control | /calendario, /tabla | mitigate | Both routes inside the existing `ProtectedRoute` layout — unauthenticated users redirected to /login (Phase 1 INF-02 pattern) |
| T-02-04-SC | Tampering | package installs | accept | No new packages installed |
</threat_model>

<verification>
- `/tabla` loads from `leaderboard_view`, ranked by points, with own-user highlight and empty state.
- `/calendario`/`/tabla` are protected; `/` and `/bienvenido` redirect to `/calendario`; auth redirects updated.
- Navbar links present with active state.
- After `calculate_prediction_points`, the leaderboard reflects updated points (QUI-04 → QUI-05).
- `npm run build` passes.
</verification>

<success_criteria>
- Phase 2 is fully reachable: login → /calendario → predict → /tabla shows ranked points after admin scoring. QUI-04 and QUI-05 satisfied.
</success_criteria>

<output>
Create `.planning/phases/02-calendar-quinielas/02-04-SUMMARY.md` when done.
</output>
