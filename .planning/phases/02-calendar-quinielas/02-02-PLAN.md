---
phase: 02-calendar-quinielas
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: []
autonomous: false
requirements: [CAL-01]
must_haves:
  truths:
    - "Migration 0002 is applied to the live Supabase database"
    - "All 72 matches are loaded into the matches table"
    - "place_prediction and calculate_prediction_points are callable on the live DB"
    - "leaderboard_view returns rows for authenticated callers"
  artifacts:
    - path: "supabase/migrations/0002_matches_predictions.sql"
      provides: "applied migration (verified live)"
      contains: "create table public.matches"
  key_links:
    - from: "local migration + seed"
      to: "live Supabase Postgres"
      via: "supabase db push + seed load"
      pattern: "supabase db push"
---

<objective>
Apply migration `0002_matches_predictions.sql` to the live Supabase database and load the 72-match seed. Phase 2 verification CANNOT pass without the schema live — the frontend slices in Plans 03/04 query real tables.

Purpose: The DB push requires an interactive terminal / `SUPABASE_ACCESS_TOKEN` and is the mandatory [BLOCKING] gate between backend authoring (Plan 01) and frontend slices (Plans 03/04).

Output: Live `matches` (72 rows) and `predictions` tables, RLS, `leaderboard_view`, and both SQL functions, confirmed on the linked project.
</objective>

<execution_context>
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@D:/sultanes_fubol/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-calendar-quinielas/02-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Push migration 0002 and load the WC2026 seed</name>
  <files>supabase/migrations/0002_matches_predictions.sql, supabase/seed/matches_wc2026.sql</files>
  <read_first>
    - supabase/migrations/0002_matches_predictions.sql (the migration to be applied)
    - supabase/seed/matches_wc2026.sql (the seed to load)
    - .planning/phases/02-calendar-quinielas/02-RESEARCH.md §Environment Availability (project linked: pajowyfyvdscyqebbhkv)
  </read_first>
  <action>
    The user runs this manually in a terminal (it requires a TTY / `SUPABASE_ACCESS_TOKEN` and writes to the live DB; Claude cannot run it non-interactively). Steps: (1) at the repo root run `supabase db push` against project `pajowyfyvdscyqebbhkv` to apply `0002_matches_predictions.sql` — set `SUPABASE_ACCESS_TOKEN` first if prompted to log in; (2) load the seed via `supabase db execute --file supabase/seed/matches_wc2026.sql`, or paste the contents of `supabase/seed/matches_wc2026.sql` into the Supabase dashboard SQL editor and run it; (3) run the verification count queries in the dashboard. Do NOT advance to Plans 03/04 until `select count(*) from public.matches` returns 72 and both SQL functions are present.
  </action>
  <what-built>
    Plan 01 produced migration `0002_matches_predictions.sql` (matches + predictions tables, RLS, leaderboard_view, leaderboard_points column, place_prediction + calculate_prediction_points functions) and the 72-row seed `matches_wc2026.sql`. These must now be applied to the live Supabase project. This step requires a terminal because `supabase db push` is non-TTY-hostile and may require `SUPABASE_ACCESS_TOKEN`; the user runs it manually.
  </what-built>
  <how-to-verify>
    1. In a terminal at the repo root, run: `supabase db push` (set `SUPABASE_ACCESS_TOKEN` env var first if prompted for login). Confirm the project linked is `pajowyfyvdscyqebbhkv`.
    2. Load the seed: `supabase db execute --file supabase/seed/matches_wc2026.sql` (or paste the seed SQL into the Supabase dashboard SQL editor and run it).
    3. In the Supabase dashboard SQL editor, run `select count(*) from public.matches;` — expect `72`.
    4. Run `select count(distinct group_name) from public.matches;` — expect `12` (groups A–L).
    5. Confirm the functions exist: in the dashboard, check Database → Functions for `place_prediction` and `calculate_prediction_points`.
    6. Confirm `select * from public.leaderboard_view limit 1;` returns columns username, tokens, leaderboard_points (and no is_admin/is_blocked).
  </how-to-verify>
  <verify>
    <human-check>Migration applied with no errors; `select count(*) from public.matches` returns 72; `select count(distinct group_name) from public.matches` returns 12; both SQL functions visible; leaderboard_view queryable.</human-check>
  </verify>
  <acceptance_criteria>
    - `supabase db push` completes without error against project pajowyfyvdscyqebbhkv.
    - `matches` table contains exactly 72 rows.
    - 12 distinct groups (A–L) present.
    - `place_prediction` and `calculate_prediction_points` exist on the live DB.
    - `leaderboard_view` is queryable and exposes only safe columns.
  </acceptance_criteria>
  <done>Migration applied; `select count(*) from public.matches` returns 72; 12 distinct groups; both SQL functions and leaderboard_view present and queryable on the live DB.</done>
  <resume-signal>Type "approved" once `select count(*) from public.matches` returns 72 and both functions exist, or describe the error encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer terminal → Supabase project | Migration DDL + seed DML applied via authenticated CLI / dashboard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-T | Tampering | live migration | mitigate | Human reviews the migration file before push; count checks (72 rows, 12 groups) confirm correct application |
| T-02-02-I | Information Disclosure | SUPABASE_ACCESS_TOKEN | mitigate | Token set as env var in the user's terminal only; never committed or exposed to frontend (CLAUDE.md) |
| T-02-02-SC | Tampering | package installs | accept | No package installs in this plan |
</threat_model>

<verification>
- `matches` table has 72 rows across 12 groups on the live DB.
- Both SQL functions and the leaderboard_view exist live.
- This gate must pass before Plans 03/04 are executed.
</verification>

<success_criteria>
- Live schema matches the local migration; 72 matches loaded; functions callable.
</success_criteria>

<output>
Create `.planning/phases/02-calendar-quinielas/02-02-SUMMARY.md` when done.
</output>
