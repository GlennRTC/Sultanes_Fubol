---
phase: 05-live-match-widget
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0009_live_scores.sql
  - src/types/index.ts
autonomous: true
requirements:
  - live-scores-db
  - live-scores-types
must_haves:
  truths:
    - "Migration file 0009_live_scores.sql exists with all three SQL statements"
    - "matches table Row/Insert/Update shapes in Database type include external_match_id"
    - "Match interface includes external_match_id optional field"
  artifacts:
    - path: "supabase/migrations/0009_live_scores.sql"
      provides: "Schema: external_match_id column, index, Realtime publication"
      contains: "ALTER TABLE public.matches ADD COLUMN IF NOT EXISTS external_match_id"
    - path: "src/types/index.ts"
      provides: "TypeScript types for external_match_id"
      contains: "external_match_id"
  key_links:
    - from: "supabase/migrations/0009_live_scores.sql"
      to: "public.matches"
      via: "ALTER TABLE"
      pattern: "ALTER TABLE public\\.matches ADD COLUMN"
    - from: "src/types/index.ts"
      to: "Match interface"
      via: "external_match_id optional field"
      pattern: "external_match_id\\?"
---

<objective>
Create the DB migration that adds external_match_id to the matches table and enrolls the table in the Supabase Realtime publication. Simultaneously update the TypeScript types file to reflect the new column.

Purpose: These two artifacts are the foundation for the entire live-scores pipeline. The Edge Function writes external_match_id; the Realtime subscription in the widget listens for UPDATE events on matches. Both depend on this wave completing first.

Output: supabase/migrations/0009_live_scores.sql + updated src/types/index.ts
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/ROADMAP.md
@/home/glenn/Denv/Sultanes_Fubol/src/types/index.ts
@/home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0008_leaderboard_public.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration 0009_live_scores.sql</name>
  <files>supabase/migrations/0009_live_scores.sql</files>
  <action>Create the file supabase/migrations/0009_live_scores.sql with exactly these three SQL statements in order:

1. Add external_match_id column (per D-05 — needed for fast lookup after first sync):
   ALTER TABLE public.matches ADD COLUMN IF NOT EXISTS external_match_id integer;

2. Create index for fast lookup by external ID (per D-05 — subsequent sync passes use this):
   CREATE INDEX IF NOT EXISTS matches_external_id_idx ON public.matches (external_match_id);

3. Enroll matches table in Supabase Realtime publication (required for TodayMatchesWidget Realtime subscription — per D-01):
   ALTER PUBLICATION supabase_realtime ADD TABLE public.matches;

Do NOT add any other columns or constraints. Do NOT wrap in a transaction block — Supabase applies migrations as-is. Add a brief comment header identifying the migration purpose.</action>
  <verify>
    <automated>grep -c "external_match_id" /home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0009_live_scores.sql && grep -c "supabase_realtime" /home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0009_live_scores.sql && grep -c "matches_external_id_idx" /home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0009_live_scores.sql</automated>
  </verify>
  <done>File exists. Contains ADD COLUMN IF NOT EXISTS external_match_id integer. Contains CREATE INDEX IF NOT EXISTS matches_external_id_idx. Contains ALTER PUBLICATION supabase_realtime ADD TABLE public.matches.</done>
</task>

<task type="auto">
  <name>Task 2: Update src/types/index.ts — add external_match_id</name>
  <files>src/types/index.ts</files>
  <action>Edit src/types/index.ts to add external_match_id to all four relevant locations. Read the file first to confirm current line numbers before editing.

Location 1 — Database type, matches.Row (after "created_at: string;" in the Row block):
  external_match_id: number | null;

Location 2 — Database type, matches.Insert (after "created_at?: string;" in the Insert block):
  external_match_id?: number | null;

Location 3 — Database type, matches.Update (after "created_at?: string;" in the Update block):
  external_match_id?: number | null;

Location 4 — Match interface (after "venue: string | null;" line, before the closing brace of the Match interface):
  external_match_id?: number | null;  // set by sync-live-scores Edge Function (D-05)

Use Edit tool for each location. The Match interface is the app-level domain type used by components. The Database type shapes are for Supabase client typings. Both must stay in sync per the project convention (src/types/index.ts is the single TypeScript types file).</action>
  <verify>
    <automated>grep -c "external_match_id" /home/glenn/Denv/Sultanes_Fubol/src/types/index.ts</automated>
  </verify>
  <done>grep returns 4 — one occurrence in each of Row, Insert, Update database shapes, and the Match interface. TypeScript build passes: npx tsc --noEmit returns exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration file → Supabase | SQL applied by developer with service-role access; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | 0009_live_scores.sql | accept | Migration applied by authenticated developer via supabase CLI; file is version-controlled |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
After both tasks:
- supabase/migrations/0009_live_scores.sql exists and contains all three SQL statements
- src/types/index.ts has external_match_id in 4 locations (Row, Insert, Update, Match interface)
- npx tsc --noEmit exits 0 (no TypeScript errors introduced)
</verification>

<success_criteria>
Migration file is ready to apply. TypeScript types reflect the new column. No other files are modified in this wave.
</success_criteria>

<output>
Create .planning/phases/05-live-match-widget/05-01-SUMMARY.md when done
</output>
