---
phase: 05-live-match-widget
plan: "02"
type: checkpoint
wave: 2
depends_on:
  - "05-01"
files_modified: []
autonomous: false
requirements:
  - live-scores-schema-applied
must_haves:
  truths:
    - "external_match_id column exists in the live Supabase matches table"
    - "matches table is enrolled in the supabase_realtime publication"
    - "index matches_external_id_idx exists in the database"
  artifacts: []
  key_links: []
---

<objective>
Human checkpoint: apply migration 0009 to the live Supabase project and verify the schema changes are in effect before the Edge Function and widget are built against them.

Purpose: The Edge Function (Wave 3) writes to external_match_id and the widget (Wave 4) subscribes to Realtime on the matches table. Neither works correctly unless the DB schema is updated first. This checkpoint gates Wave 3 and 4 from starting until the schema is confirmed live.

Output: Confirmation that migration 0009 is applied and verified in the live database.
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/phases/05-live-match-widget/05-01-SUMMARY.md
@/home/glenn/Denv/Sultanes_Fubol/supabase/migrations/0009_live_scores.sql
</context>

<tasks>

<task type="checkpoint:human-action">
  <what-built>Migration file supabase/migrations/0009_live_scores.sql was created in Wave 1 with:
  - ALTER TABLE public.matches ADD COLUMN IF NOT EXISTS external_match_id integer;
  - CREATE INDEX IF NOT EXISTS matches_external_id_idx ON public.matches (external_match_id);
  - ALTER PUBLICATION supabase_realtime ADD TABLE public.matches;</what-built>
  <how-to-verify>
    Step 1 — Apply the migration via Supabase CLI (preferred):
      supabase db push
    OR via Supabase Dashboard: open SQL Editor, paste the contents of supabase/migrations/0009_live_scores.sql, and run it.

    Step 2 — Verify external_match_id column exists:
      In Supabase Dashboard → Table Editor → matches table → confirm "external_match_id" column appears with type integer, nullable.

    Step 3 — Verify Realtime publication:
      In Supabase Dashboard → Database → Replication → confirm "matches" appears in the supabase_realtime publication table list.
      OR run in SQL Editor:
        SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime' AND tablename = 'matches';
      Expected: one row returned.

    Step 4 — Verify index:
      Run in SQL Editor:
        SELECT indexname FROM pg_indexes WHERE tablename = 'matches' AND indexname = 'matches_external_id_idx';
      Expected: one row returned.
  </how-to-verify>
  <resume-signal>Type "listo" when all four verification steps pass. If any step fails, describe what you see and Claude will help troubleshoot before continuing.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer CLI → Supabase cloud DB | Authenticated supabase CLI or dashboard SQL; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | supabase db push | accept | Developer-authenticated CLI operation; migration is idempotent (IF NOT EXISTS) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
Human confirms:
- external_match_id column visible in matches table
- matches table in supabase_realtime publication
- matches_external_id_idx index exists
</verification>

<success_criteria>
All three schema changes from migration 0009 are confirmed live in the Supabase project. Wave 3 (Edge Function) and Wave 4 (Widget) may proceed.
</success_criteria>

<output>
Create .planning/phases/05-live-match-widget/05-02-SUMMARY.md when done
</output>
