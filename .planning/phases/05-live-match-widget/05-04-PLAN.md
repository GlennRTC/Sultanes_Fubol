---
phase: 05-live-match-widget
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/components/TodayMatchesWidget.tsx
  - src/pages/CalendarPage.tsx
autonomous: true
requirements:
  - live-scores-widget
  - live-scores-calendar-integration
must_haves:
  truths:
    - "Widget renders today's matches as horizontal-scroll cards at top of CalendarPage"
    - "Widget shows 'EN VIVO' badge when match status=live, 'FINALIZADO' when finished, local time when scheduled"
    - "Score shown as final numbers only when status=finished; shows ? - ? while live (D-03)"
    - "Widget hidden entirely when todayMatches array is empty (no matches today)"
    - "Collapse toggle persisted in localStorage key fubol_today_widget_open, default true (D-06)"
    - "Realtime subscription on matches table UPDATE events updates card state without page refresh (D-01)"
    - "Widget uses overflow-x-auto + shrink-0 cards for horizontal scroll at 375px (mobile)"
    - "No frontend polling — only Supabase Realtime for live updates"
  artifacts:
    - path: "src/components/TodayMatchesWidget.tsx"
      provides: "Collapsible today's matches widget with Realtime updates"
      contains: "fubol_today_widget_open"
    - path: "src/pages/CalendarPage.tsx"
      provides: "CalendarPage with widget mounted at top"
      contains: "TodayMatchesWidget"
  key_links:
    - from: "src/components/TodayMatchesWidget.tsx"
      to: "supabase.channel('matches-today')"
      via: "postgres_changes UPDATE subscription"
      pattern: "postgres_changes"
    - from: "src/pages/CalendarPage.tsx"
      to: "src/components/TodayMatchesWidget.tsx"
      via: "import and render at top of page"
      pattern: "TodayMatchesWidget"
---

<objective>
Build TodayMatchesWidget and integrate it into CalendarPage. This wave runs in parallel with Wave 3 (Edge Function) because both only depend on Wave 2 (schema applied). The widget uses Supabase Realtime — the same pattern used in ApuestasPage — to receive live score updates pushed by the Edge Function without polling.

Purpose: This is the user-facing output of the entire phase. When the cron updates match status/scores in the DB, Realtime delivers the change to the widget within seconds and the card re-renders automatically — no page refresh required (per D-01 success criteria).

Output: src/components/TodayMatchesWidget.tsx + updated src/pages/CalendarPage.tsx
</objective>

<execution_context>
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/workflows/execute-plan.md
@/home/glenn/Denv/Sultanes_Fubol/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/glenn/Denv/Sultanes_Fubol/.planning/phases/05-live-match-widget/CONTEXT.md
@/home/glenn/Denv/Sultanes_Fubol/src/pages/ApuestasPage.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/pages/CalendarPage.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/components/AdminRoute.tsx
@/home/glenn/Denv/Sultanes_Fubol/src/types/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TodayMatchesWidget component</name>
  <files>src/components/TodayMatchesWidget.tsx</files>
  <action>Create src/components/TodayMatchesWidget.tsx. Follow the import/hook style from ApuestasPage.tsx and the component structure from AdminRoute.tsx.

Imports needed:
  import { useState, useEffect } from 'react';
  import { toZonedTime, format } from 'date-fns-tz';
  import { supabase } from '../lib/supabase';
  import type { Match } from '../types/index';

The component receives one prop: timezone (string). CalendarPage already manages timezone state; pass it down.
  export function TodayMatchesWidget({ timezone }: { timezone: string }) { ... }

STATE:
  - matches: Match[] (today's matches, initially [])
  - open: boolean (collapse state, initialized from localStorage)
  - loading: boolean

INITIALIZATION of open state:
  Use lazy useState initializer: () => {
    const stored = localStorage.getItem('fubol_today_widget_open');
    return stored === null ? true : stored === 'true';
  }

INITIAL FETCH (useEffect, runs once on mount):
  Compute today UTC date range:
    const now = new Date();
    const todayStart = new Date(Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate(), 0, 0, 0)).toISOString();
    const todayEnd   = new Date(Date.UTC(now.getUTCFullYear(), now.getUTCMonth(), now.getUTCDate(), 23, 59, 59)).toISOString();
  Query:
    supabase.from('matches').select('*').gte('match_datetime', todayStart).lte('match_datetime', todayEnd).order('match_datetime')
  On success: setMatches(data ?? [])
  On error: console.error, leave matches as []
  Always: setLoading(false)

REALTIME SUBSCRIPTION (useEffect, runs once on mount — separate from fetch):
  Follow the exact pattern from ApuestasPage.tsx lines 66–94.
  Channel name: 'matches-today'
  Event: UPDATE on schema public, table 'matches'
  Handler payload type: the new record shape is Match (cast payload.new as Match).
  On UPDATE event: update matching row in local matches state:
    setMatches(prev => prev.map(m => m.id === updated.id ? updated : m))
  Cleanup: return () => { supabase.removeChannel(channel); }

TOGGLE HANDLER:
  function handleToggle() {
    setOpen(prev => {
      const next = !prev;
      localStorage.setItem('fubol_today_widget_open', String(next));
      return next;
    });
  }

LOCAL TIME HELPER:
  function formatMatchTime(match: Match): string {
    const zoned = toZonedTime(new Date(match.match_datetime), timezone);
    return format(zoned, 'HH:mm', { timeZone: timezone });
  }

CARD CONTENT helper (returns JSX elements inline — do not extract to separate component):
  Status badge:
    - status === 'live'     → span with "🟢 EN VIVO" text, text-green-400 font-bold text-xs
    - status === 'finished' → span with "✅ FINALIZADO" text, text-slate-400 text-xs
    - status === 'scheduled'→ span with "⏰ {formatMatchTime(match)}" text, text-slate-400 text-xs
  Score display (only when status is 'live' or 'finished'):
    - live:     show "? – ?" in text-slate-300 font-mono (per D-03: free tier has no in-play score)
    - finished: show "{home_score} – {away_score}" in text-slate-100 font-bold font-mono
    - scheduled: show nothing (no score row)

RENDER (return):
  If matches.length === 0 and !loading: return null (widget hidden, per success criteria #4).

  Outer wrapper: div with classes "mb-4" (spacing before calendar content)

  Header bar: div with "flex items-center justify-between px-3 py-2 bg-slate-800 rounded-t-lg border border-slate-700 cursor-pointer" + onClick={handleToggle}
    Left: span "Partidos de hoy" with "text-sm font-bold text-slate-100"
    Right: chevron svg that rotates 180deg when open (use "transition-transform" + rotate-180 class when open)

  Collapsible body (shown when open === true): div with classes "border-x border-b border-slate-700 rounded-b-lg overflow-hidden"
    Inner scroll container: div with "flex gap-3 overflow-x-auto px-3 py-3"
    For each match in matches:
      Card div: "shrink-0 w-40 bg-slate-700 rounded-lg px-3 py-3 flex flex-col gap-1"
        Status badge (top)
        Home team: p "text-slate-100 text-sm font-medium truncate"
        Score row (middle, when applicable as described above)
        Away team: p "text-slate-100 text-sm font-medium truncate"

  Loading state: while loading=true render a single-line skeleton placeholder instead of the scroll container:
    div "h-24 bg-slate-800 rounded-lg animate-pulse border border-slate-700"</action>
  <verify>
    <automated>test -f /home/glenn/Denv/Sultanes_Fubol/src/components/TodayMatchesWidget.tsx && grep -c "fubol_today_widget_open" /home/glenn/Denv/Sultanes_Fubol/src/components/TodayMatchesWidget.tsx && grep -c "postgres_changes" /home/glenn/Denv/Sultanes_Fubol/src/components/TodayMatchesWidget.tsx && grep -c "overflow-x-auto" /home/glenn/Denv/Sultanes_Fubol/src/components/TodayMatchesWidget.tsx && grep -c "shrink-0" /home/glenn/Denv/Sultanes_Fubol/src/components/TodayMatchesWidget.tsx</automated>
  </verify>
  <done>File exists. Contains fubol_today_widget_open localStorage key. Contains postgres_changes Realtime subscription. Contains overflow-x-auto for horizontal scroll container. Contains shrink-0 on cards. npx tsc --noEmit exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate TodayMatchesWidget into CalendarPage</name>
  <files>src/pages/CalendarPage.tsx</files>
  <action>Edit src/pages/CalendarPage.tsx to import and render TodayMatchesWidget at the top of the page content, above the timezone bar.

Step 1 — Add import (after the existing imports block, before the GROUPS constant):
  import { TodayMatchesWidget } from '../components/TodayMatchesWidget';

Step 2 — Render the widget. In the JSX return, insert TodayMatchesWidget immediately after the opening div.max-w-3xl and BEFORE the h1 "Calendario" heading:
  Insert: <TodayMatchesWidget timezone={timezone} />
  The timezone state variable already exists in CalendarPage (line 43 of the current file).

No other changes to CalendarPage. Do not modify any existing state, handlers, or other JSX. The widget is self-contained — it fetches its own data and manages its own Realtime subscription.</action>
  <verify>
    <automated>grep -c "TodayMatchesWidget" /home/glenn/Denv/Sultanes_Fubol/src/pages/CalendarPage.tsx && npx tsc --noEmit --project /home/glenn/Denv/Sultanes_Fubol/tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>CalendarPage imports TodayMatchesWidget. CalendarPage renders TodayMatchesWidget with timezone prop above the h1. npx tsc --noEmit exits 0. npm run build exits 0 (Vite build succeeds with no errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Realtime → widget | Realtime delivers UPDATE payloads from DB; widget treats payload.new as Match shape |
| user browser → localStorage | localStorage stores only a boolean (open/closed); no sensitive data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Tampering | Realtime payload | accept | Widget only reads match status/scores from payload; no writes triggered from widget. Payload data originates from our own DB (updated by service-role Edge Function). |
| T-05-08 | Information Disclosure | FOOTBALL_DATA_API_KEY | accept | Key is never referenced in frontend; widget uses supabase client with anon key only |
| T-05-09 | DoS (resource) | Realtime subscription | mitigate | Single channel 'matches-today', cleaned up on unmount (removeChannel). Follows same pattern as ApuestasPage which is already in production. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies; uses existing react, supabase-js, date-fns-tz already in package.json |
</threat_model>

<verification>
After both tasks:
- TodayMatchesWidget.tsx exists and passes all grep checks
- CalendarPage.tsx has TodayMatchesWidget import and render (grep returns 2: import + usage)
- npx tsc --noEmit exits 0
- npm run build exits 0
</verification>

<success_criteria>
Widget component is complete with Realtime subscription, localStorage toggle, horizontal scroll, conditional status badges, and correct score display logic (? while live, final score when finished). CalendarPage renders the widget at the top. No new npm dependencies introduced. FOOTBALL_DATA_API_KEY is never referenced anywhere in frontend source.
</success_criteria>

<output>
Create .planning/phases/05-live-match-widget/05-04-SUMMARY.md when done
</output>
